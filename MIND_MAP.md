# Mind Map Format - Self-Documentation

> **For AI Agents:** This mind map is your primary knowledge index. Start with the Gnostica project anchors [6-10], then follow links [N] to find what you need; consult format nodes [1-5] only when you need the mind-map rules. Always reference node IDs. When you encounter bugs, document your attempts in relevant nodes. When you make changes, update outdated nodes immediately—especially project overview nodes since they're your springboard. Add new nodes only for genuinely new concepts. Keep it compact (20-50 nodes typical). The mind map wraps every task: consult it, rely on it, update it. Never change the first 5 numbered points.

> **Project Navigation Note:** In this Gnostica file, nodes [1-5] are preserved generic format references, not the project's daily navigation hubs. For project work, start at Gnostica anchors [6-10]; any generic wording inside [1-5] about overview nodes describes the mind-map format in general, not this repository's project anchors.

[1] **Mind Map Format Overview** - A graph-based documentation format stored as plain text files where each node is a single line containing an ID, title, and inline references [2]. The format leverages LLM familiarity with citation-style references from academic papers, making it natural to generate and edit [3]. It serves as a superset structure that can represent trees, lists, or any graph topology [4], scaling from small projects (<50 nodes) to complex systems (500+ nodes) [5].

[2] **Node Syntax Structure** - Each node follows the format: `[N] **Node Title** - node text with [N] references inlined` [1]. Nodes are line-oriented, allowing line-by-line loading and editing by AI models [3]. The inline reference syntax `[N]` creates bidirectional navigation between concepts, with links embedded naturally within descriptive text rather than as separate metadata [1][4]. This structure is both machine-parseable and human-readable, supporting grep-based lookups for quick node retrieval [3].

[3] **Technical Advantages** - The format enables line-by-line overwriting of nodes without complex parsing [2], making incremental updates efficient for both humans and AI agents [1]. Grep operations allow instant node lookup by ID or keyword without loading the entire file [2]. The text-based storage ensures version control compatibility, diff-friendly editing, and zero tooling dependencies [4]. LLMs generate this format naturally because citation syntax `[N]` mirrors academic paper references they've seen extensively during training [1][5].

[4] **Graph Topology Benefits** - Unlike hierarchical trees or linear lists, the graph structure allows many-to-many relationships between concepts [1]. Any node can reference any other node, creating knowledge clusters around related topics [2][3]. The format accommodates cyclic references for concepts that mutually depend on each other, captures cross-cutting concerns that span multiple subsystems, and supports progressive refinement where nodes are added to densify understanding [5]. This flexibility makes it suitable as a universal knowledge representation format [1].

[5] **Scalability and Usage Patterns** - Small projects typically need fewer than 50 nodes to capture core architecture, data flow, and key implementations [1]. Complex topics or large codebases can scale to 500+ nodes by adding specialized deep-dive nodes for algorithms, optimizations, and subsystems [4]. The methodology includes a bootstrap prompt for generating initial mind maps from existing codebases automatically [1]. Scale is managed through overview nodes [1-5] that serve as navigation hubs, with detail nodes forming clusters around major concepts [3][4]. The format remains navigable at any scale due to inline linking and grep-based search [2][3].

[6] **Gnostica App Overview** - Gnostica is a Clojure/ClojureScript implementation of the tarot-and-Icehouse territory game documented in `docs/rules.txt` [7]. `src/main/gnostica/app.cljs` is the browser shell/startup entry point; `src/main/gnostica/app/events.cljs` preserves stable `:gnostica.app/...` re-frame ids and feature view-model subscriptions; `src/main/gnostica/app/handlers.cljc` keeps event-level transitions testable with injected shuffle seeds; `src/main/gnostica/fixtures.cljc` owns local lobby/demo and smoke fixtures; and focused `src/main/gnostica/ui/*.cljs` namespaces render the lobby, board, overlays, move panel, header, cards, and help dialogs [9][22][23][24][25][30][44][45][48]. Shared `.cljc` namespaces hold board, app-db, move-selection, game-state, schema, layout, icon, keyboard shortcut, piece, fixture, deterministic-shuffle, and major sequencing contracts [8][16][17][18][19][21][22][24][44][45][48][49]. The browser surface combines a local setup lobby, full-window Three.js board with CSS fallback, card/move/territory overlays, score/end-turn/challenge controls, keyboard help, and special move icon guide [11][12][13][14][15][22][23][24][25][30][48]. Implemented browser-free gameplay now includes setup/bids, draw/discard turns and draw majors, Cup/Rod/Disc/Sword suit moves, composite suit majors for Empress/Emperor/Lovers/Chariot/Hanged Man/Temperance, Sun/Strength/Star/Tower/Justice/Death/Moon composites, manipulation majors, World delegation, orient/initial placement, and endgame flow; browser staging includes World copy selection plus the currently wired move-selection powers, while explicit unavailable results remain the fallback for unwired powers [23][24][35][37][40][41][42][43][45][46][47][48][50][51][52][53].

[7] **Rules Source** - `docs/rules.txt` is the local implementation rules reference, while the upstream Gnostica PDF at `https://www.looneylabs.com/sites/default/files/pyramid_rules/Gnostica.pdf` is retained as provenance. If those sources differ, treat `docs/rules.txt` as authoritative for current repo behavior until it is deliberately reconciled against the PDF. Implemented rule slices include deterministic setup, official target scores and starting bids, six-card hands, nine-card territory setup, card-zone accounting, draw/discard turns and draw majors, scoring/challenge/victory flow, Cup/Rod/Disc/Sword moves, Wheel/Empress/Emperor variants, Strength/Star/Sun/Tower/Justice/Death/Moon composites, Hierophant/Hermit/Devil manipulation, composite suit majors for Empress, Emperor, Lovers, Chariot, Hanged Man, and Temperance, World copying of major territories with browser staging, orient-piece, initial placement, and browser confirmation wiring for implemented staged powers [8][9][17][23][24][34][35][37][40][41][42][43][46][47][48][50][51][52][53].

[8] **Board Model** - `src/main/gnostica/board.cljc` defines the direct initial 3x3 board helper. When called directly, `initial-board` shuffles the provided deck with its default shuffle or an injected shuffle function before taking nine table cards, records each cell's row, column, `:face :up`, and assigns checkerboard orientation with top-left portrait so every orthogonal neighbor is opposite [7][10]. The authoritative `create-game` setup path orders or shuffles the full deck once, deals player hands first, then calls `initial-board` with `identity` on the remaining board deck so board setup does not double-shuffle or consume cards before hands [17][34].

[9] **Board Rendering** - `src/main/gnostica/ui/board.cljs` renders the board area from the composed `:gnostica.app/board-view` subscription, chooses between `gnostica.three-board/scene` and the CSS fallback, passes renderer callbacks, dispatches stable selection events registered in `src/main/gnostica/app/events.cljs`, and renders an empty pending board state safely before the lobby has created `:game` [11][12][13][14][17][25]. `board-view` caches board cells, pieces, pieces-by-space keyed by `gnostica.pieces/territory-space` or `wasteland-space`, wastelands, board-space bounds, selected index, icon mode, Three.js runtime status, renderer errors, and derived renderer availability so fallback layout and capability decisions stay out of the view render body [16][18][25][29]. `src/main/gnostica/app.cljs` composes a local lobby screen before game start, then a full-window board shell with closable overlay card zones and side-docked move/territory panels driven by `:open-panels` [6][23][24]. The normal board surface is `gnostica.three-board/scene`, with Three.js runtime gating, camera controls, raycast selection, and browser-free layout math split across focused nodes [11][12][13][14][18][26][27]. The CSS `.board-stage` path remains the fallback board surface with a matching responsive velvet-to-black fade, board-card pieces, wasteland piece markers, and selection behavior aligned with the 3D path [25]. The surrounding UI lets the board keep the viewport, derives card zones from `:game`, and stages move choices outside `:game` [23][24][37].

[10] **Test Runner** - `clojure -M:test` runs `gnostica.test-runner`, which loads card, board, board-layout, app-state, fixture, piece, game-schema, game-state, keyboard-shortcut catalog, and gameplay feature tests [8][16][17][18][19][20][22][28][29][44]. Unit coverage is grouped by pure rendering math, card metadata, fixture data, piece geometry, app-db derivations, shortcut command matching, game-state transitions, schema invariants, and feature-runner scenarios [20][22][28][29][44]. Browser release and smoke coverage for the Three.js board, fallback paths, card zones, and icon mode behavior live outside the unit runner [15][31].

[11] **Three.js CDN Runtime** - `src/main/resources/index.html` loads Three.js plus `examples/js/controls/OrbitControls.js` from pinned `three@0.128.0` CDN URLs before `/js/main.js` with SRI hashes and `crossorigin="anonymous"`, preserving the npm-free shadow-cljs workflow [6]. `gnostica.three-board.runtime/runtime-status` requires `THREE.REVISION` `"128"` and `THREE.OrbitControls`, and `src/main/gnostica/app/events.cljs` captures that status through a re-frame coeffect during initialization so `:gnostica.app/board-view` drives Three.js versus CSS fallback rendering from app-db data [9][25][45]. `src/main/externs/three.ext.js` declares the global and advanced-compilation APIs used by the 3D renderer [12][13][27].

[12] **Three.js Board Scene** - `gnostica.three-board/scene` is the public Reagent component that renders the 3D board from shared board cells and piece state instead of duplicating game data [8][9][16][18]. Its implementation is split under `src/main/gnostica/three_board/`: `runtime.cljs` gates the CDN globals, `resources.cljs` creates the WebGL renderer and disposes renderer/listener/material resources, `controls.cljs` owns camera metadata, OrbitControls configuration, reset, and keyboard panning, `pointer.cljs` owns raycast card picking, `scene_graph.cljs` builds table/card/wasteland/piece meshes from stable territory and wasteland space keys, and `lifecycle.cljs` orchestrates mount/update/unmount around those contracts [11][13][14]. Scene metadata counts only pieces whose current space is a drawable territory or wasteland, so smoke can compare visible piece count to rendered edge outlines [15][32]. Card texture composition remains in `gnostica.three-card-textures`, while renderer detail behavior is documented in focused nodes [26][27][30].

[13] **3D Camera Controls** - `gnostica.three-board.controls` configures the z-up perspective camera and `THREE.OrbitControls` used by `gnostica.three-board/scene`, keeping the default view over the 3x3 board [11][12]. Controls enable mouse, trackpad, and touch orbit/zoom, disable pointer pan, clamp distance and polar angle to allow closer card inspection without clipping through pieces, expose zoom clamp plus camera-distance/target metadata for smoke verification, support focused WASD/arrow-key bounded board-plane panning, preserve the active camera distance and target when icon-mode toggling remounts card textures, and expose a `Reset view` button that restores the saved default camera state [9][15][22].

[14] **3D Card Selection** - `gnostica.three-board.scene-graph` assigns each card mesh a board index from `src/main/gnostica/board.cljc`, while `gnostica.three-board.pointer` uses `THREE.Raycaster` on click-like pointer releases to pick card planes after camera movement and invokes the selection callback supplied by `gnostica.ui.board` so the territory panel stays in sync through the stable `:gnostica.app/select-board-card` event [8][9][12]. Each card also has a slightly larger hidden highlight plane that follows the card orientation and becomes visible for the selected index; drag distance filtering prevents normal orbit gestures from changing selection [13].

[15] **3D Board Verification** - The CDN-based 3D board path is verified with `clojure -M:test` for pure board/layout/piece invariants, `clojure -M:release` for advanced compilation against `src/main/externs/three.ext.js`, and `clojure -M:smoke` for automated browser coverage [10][11][16][18]. The default smoke path starts the released Ring app and therefore needs a current `src/main/resources/js/main.js` from `clojure -M:release`; `SMOKE_URL` can target an existing dev or release server instead [32]. Smoke coverage is split between reusable Chrome/CDP harness code, Gnostica page-stat/readiness checks, and high-level scenario orchestration for Three.js runtime readiness, nonblank rendering, card zones, icon toggles, card selection, and fallback paths [25][31][32].

[16] **Icehouse Piece Model** - `src/main/gnostica/pieces.cljc` defines six metadata-backed player colors, piece sizes/pips, legal orientations, stable `territory-space` and `wasteland-space` keys for grouping active pieces, compatibility helpers for territory-index lookups, and square-pyramid geometry helpers for cardinal pieces lying on a side face [6][10][18]. Pieces with `:space-index` group under territory keys and pieces with `:space {:kind :wasteland ...}` group under wasteland coordinate keys, letting app-state, CSS fallback, and Three.js render both legal occupancy forms [9][12][25][29]. Local visible demo pieces live in `gnostica.fixtures/demo-board-pieces`, smoke-only wasteland coverage lives in `gnostica.fixtures/smoke-board-pieces`, and `gnostica.app-state` injects them into `[:pieces :on-board]` only when explicit init options provide `:demo-board-pieces`, keeping player stashes and `[:pieces :stashes]` reconciled through `gnostica.game-state/with-board-pieces` [9][17][36][44]. `gnostica.ui.board` passes the derived board-piece collection into the renderer; Three.js and CSS rendering details live in focused nodes [27][33].

[17] **Game State Contract** - `src/main/gnostica/game_state.cljc` is the browser-free public facade for tests, Gherkin-style feature steps, and re-frame events, while `src/main/gnostica/game_state/core.cljc` owns deterministic game construction, deck accounting, player metadata, turn metadata, shared piece/stash helpers, draw/discard piles, target score, official starting-bid resolution, scoring, challenge/end-turn resolution, setup hooks, and compact history events [6][10][19][34][48]. `with-board-pieces` is the shared fixture/test seeding helper that replaces active pieces and rebuilds both stash mirrors from the starting stash size [16][36][44]. Rule helpers return explicit success/error maps and live in focused transition namespaces: draw/discard plus Fool/High Priestess/Judgement in `gnostica.game-state.draw` [42][50], Cup in `gnostica.game-state.cup` [35], Rod in `gnostica.game-state.rod` [40][41], Disc/Strength/Star/Sun in `gnostica.game-state.disc` [46], Sword/Justice/Death/Tower/Moon in `gnostica.game-state.sword` [47], Empress/Emperor/Lovers/Chariot/Hanged Man/Temperance in `gnostica.game-state.composite` [52], Hierophant/Hermit/Devil in `gnostica.game-state.manipulation` [51], World delegation in `gnostica.game-state.world` [53], orient/initial placement in `gnostica.game-state.placement` [43], and reusable major-source sequencing in `gnostica.game-state.major` [49]. The facade re-exports the stable API used by `gnostica.app-state/initialize`, lobby start-game, and end-turn/challenge transitions, while `gnostica.app.handlers` injects seeded deterministic shuffle functions for browser setup and draw reshuffles [9][23][24][44][45][48].

[18] **Board Layout Helpers** - `src/main/gnostica/board_layout.cljc` contains browser-free renderer math shared by the Three.js board scene, app-state/move-selection board-index lookup, and tests [9][10][12][24]. It defines card dimensions, territory gaps, table size, table fade margins/planes, `cells-by-index`/`cell-by-index` stable board-index lookup, card centers, wasteland detection, board-space bounds, outline segments, visible piece slot limits, side-face pip marker descriptors, compass rotations, side-face roll, and piece heights [12][16][26][27]. Keep deterministic placement math here; leave DOM, WebGL, OrbitControls, and raycasting in the `gnostica.three-board.*` renderer namespaces, and card image fitting plus Canvas icon texture composition in `gnostica.three-card-textures` [11][13][14][30].
[19] **Gameplay State Schemas** - `src/main/gnostica/game_schema.cljc` adds Malli-backed schemas for pure gameplay data using `metosin/malli` from `deps.edn` [17]. It defines reusable schemas for card references, board cells including post-setup territory growth or destruction, six-card hands, card piles, player state with score/challenge/elimination flags, Icehouse pieces with exactly one territory or wasteland location, turn metadata, official target scores, setup data, winner data, history events, and the whole `GameState` map [8][16][17][48]. Whole-state invariant predicates and public validation helpers are split out for scanability, including readable errors for ambiguous/missing piece locations, stale wasteland coordinates, unsupported target scores, and broader state-accounting failures [36].
[20] **Gameplay Feature Harness** - `features/` contains Gherkin-style gameplay rule scenarios run by `test/gnostica/gameplay_feature_test.clj` through `gnostica.feature-runner` [10][17][19]. `test/gnostica/feature_world.clj` centralizes deterministic player setup, identity deck order, Rod, Disc, draw-major, manipulation-major, and endgame fixture setup, pure action application, state lookup, deck accounting, and Malli explanations, while `test/gnostica/feature_steps.clj` maps Given/When/Then text to helpers [17][19][41][46][48][50][51]. Feature discovery runs every `.feature` file under `features/`; current scenarios cover two-through-six-player setup; Rod minion movement, piece pushing, territory pushing, pip limits, full/void destination rejection, enemy-occupied territory-push rejection, own-wasteland landing behavior, and owned/enemy orientation rules; Disc piece growth, territory growth, hand-source discard behavior, missing-resource rejections, enemy-occupied territory rejection, Star discard-pile replacement, and a Strength shortcut; Fool skipped reveals, High Priestess redraw accounting, and Judgement hand-limit rejection; Hierophant replacement, Hermit piece/territory relocation, and Devil retargeting examples; and endgame score, challenge victory, and failed-challenge elimination examples [10][29][38][46][48][50][51].
[21] **Gnostica Icon Stacks** - `src/main/gnostica/icons.cljc` is the browser-free source for Stickas major-arcana icon stacks, icon ids, labels, and the `GnosticaStickas.pdf` reference URL [30]. `src/main/gnostica/cards.cljc` attaches each major card's ordered one-, two-, or three-icon `:gnostica-icons` vector while minors deliberately have none, and it also exposes bid rank, Cup, Rod, Disc, Sword, one-point, and territory point-value helpers used by gameplay helpers [17][28][34][40][46][47]. Layout, rendering modes, hotkey integration, and verification are documented separately so icon-stack updates do not require editing one dense node [22][30][31].
[22] **Keyboard Command Help** - `src/main/gnostica/app_state.cljc` keeps browser-free `:hotkey-help-open?` and `:icon-help-open?` flags plus open/close helpers and pure header/help view-model helpers, `src/main/gnostica/keyboard_shortcuts.cljc` defines the shared hotkey command catalog plus pure global key matching, `src/main/gnostica/ui/header.cljs` renders header controls from `:gnostica.app/header-view`, `src/main/gnostica/ui/help.cljs` renders both dialogs from `:gnostica.app/help-dialogs-view` and the shared catalog, and `src/main/gnostica/app/keyboard.cljs` owns the global keydown listener controller registered through the `:gnostica.app/install-keyboard-shortcuts` re-frame effect path [6][9][21][30][45]. Supported browser shortcuts are `?` to show keyboard commands, `G` to show detailed special move icon explanations, `I` to toggle card icon overlays without resetting the current 3D OrbitControls view, board-local WASD/arrow keys to move the focused 3D view, and `Esc` to close open help dialogs; keep the shared shortcut catalog, visible command list, keyboard listener, unit tests, smoke checks, README, and AGENTS notes synchronized when adding shortcuts [10][13][15][21].
[23] **Current-Player Card Zones** - `gnostica.app-state/card-zones` derives the current hand, draw pile, discard pile, counts, and top discard card from the authoritative `:game` value rather than duplicating zones at the app-db top level [9][17]. `gnostica.app.events` keeps the lower-level `:card-zones` subscription for compatibility and exposes `:gnostica.app/card-zones-view` to compose current player, icon mode, and zone data for `gnostica.ui.card-zones` [6][29]. `gnostica.ui.card-zones` renders a closable bottom overlay with hand card images/titles, a facedown draw deck affordance with remaining count, and a discard pile empty state or top card while reusing shared card-face helpers from `gnostica.ui.card`; visibility is controlled by `:open-panels` rather than duplicating card data [6][9][30]. Standalone draw confirmations call `apply-draw-move`, which can discard selected hand cards, draw toward the six-card limit, and reshuffle the discard pile when the draw pile is exhausted; Cup hand-card moves discard through `apply-cup-move`, Rod hand-card minion, piece, and territory moves discard through `apply-rod-move`, and Disc hand-card growth moves discard through `apply-disc-move` via app-state confirmation [35][37][41][42][46].
[24] **Move Selection UI** - `gnostica.app-state` keeps staged move-selection state outside `:game` under `:move-selection` and overlay visibility under `:open-panels`, but delegates the browser-free state machine to `gnostica.move-selection`; `gnostica.ui.move-panel` renders the closable side-docked browser controls from the composed `:gnostica.app/move-panel-view` subscription and dispatches stable `:gnostica.app/...` events [9][17]. `move-panel-view` aggregates current player, selection, prompt, ready state, board cells, selected power, and all move-control option lists so the view no longer dereferences each option subscription while deciding which controls to display [29][37]. The move-selection namespace owns source choice, source territory, hand card, implemented power, minion, ordered composite major action steps, Rod mode, Strength Disc action count, Star minion orientation, Sun Disc mode, Fool reveal count, High Priestess redraw controls, Judgement discard-pile card selection, Disc/Sword target kind, Hierophant/Devil target piece, Hermit target and destination, target piece, existing target territory, target wastelands, Wheel territory-card source, Disc/Sword/Sun replacement source/card, one-point territory card, Sword damage, distance, draw discard-card choices, draw count, orientation, confirmation, cancellation, option derivation, command construction, and structured error/result handling; `app-state` remains the app-db facade and board-inspection boundary, resolves selected/source/target territories by stable cell `:index` through `gnostica.board-layout/cell-by-index` instead of vector position, opens the territory overlay on board inspection, and confirmation can receive explicit transition options such as a draw or draw-major `:shuffle-fn` from `gnostica.app.handlers` [18][37][45]. Cup target-piece choices are enemy pieces on existing territories, Rod target-piece choices can include own or enemy pieces, Disc/Sword target/replacement options are filtered through pure resolvers plus card-value helpers, Strength action counts filter territory replacements by +1/+2 card value, Star minion orientation is simulated for target option derivation before confirmation, ordered composite major controls collect completed first actions before staging their next Cup/Rod/orient/hand-trade step, draw-major controls stage Fool skipped reveals, High Priestess redraw passes, and Judgement discard draws, and manipulation-major controls derive legal Hierophant/Devil piece targets plus Hermit piece/territory destinations from the shared major-source target helper [35][40][46][47][50][51].
[25] **CSS Board Fallback** - The `.board-stage` CSS board remains usable when required CDN globals are unavailable, the loaded Three.js revision is not r128, or WebGL renderer creation fails [9][11]. It preserves row/column card placement, the matching velvet-to-black mat fade, faint dotted wasteland outlines, visible color/orientation/pip piece markers on territories and occupied wastelands, and selected-territory clicks [14][16]. The smoke runner covers blocked CDN scripts, injected revision drift, and a smoke-only wasteland piece marker so fallback behavior stays aligned with the 3D path [15][32][44].
[26] **Three.js Card and Wasteland Rendering** - `gnostica.three-board/scene` renders nine face-up card planes with `/images/*.png` textures over a canvas-textured table plane that remains velvet under playable spaces and fades to black beyond wastelands, requests WebGL antialiasing, exposes actual context antialias metadata, uses white card material plus sRGB texture/output encoding from `gnostica.three-card-textures`, rotates landscape meshes 90 degrees, and spaces card centers with `gnostica.board-layout/card-position` so orthogonal cards never touch [7][12][15][18][30]. It draws faint dashed line outlines for browser-free `wasteland-spaces` below cards and pieces using r128 `BufferGeometry`, `Line`, and `LineDashedMaterial`, leaving raycasting targeted only at card meshes [14][18].
[27] **Three.js Piece Meshes** - Three.js renders Icehouse pieces as lit four-sided cone meshes over occupied cards, with size, color, up/cardinal orientation, child black `EdgesGeometry`/`LineSegments` outlines, and child circle pip markers derived from `gnostica.pieces` plus `gnostica.board-layout` side-face descriptors [12][16][18]. Lying/cardinal pyramids are rolled and tilted so the contact face sits flush above the card while preserving compass direction and card raycast selection [14][16][18].
[28] **Core Unit Test Coverage** - Card tests verify the 78-card manifest, classpath image resources, major icon metadata, Cup/Rod/Disc/Sword/one-point predicates, and territory point values [21][40][46][47]. Board tests verify nine face-up cards, injected shuffle use, and alternating orthogonal orientations [8]. Fixture tests verify demo piece metadata/capacity/filtering, browser lobby init options, smoke lobby bypass options including the smoke wasteland piece, and the major-icons smoke deck order, while board-layout and piece tests cover renderer coordinates, wasteland detection, board-space bounds, board-index lookup, stable territory/wasteland piece grouping, visible piece slot limits, pip marker counts/positions, compass rotations, piece height math, legal sizes/orientations, and lying square-pyramid side-face geometry [16][18][44].
[29] **Game and App-State Test Coverage** - App-state tests verify initialization under `:game`, local lobby initialization without `:game`, lobby add/remove/count limits, display-name editing, duplicate colour rejection, lobby-only shared local-controller metadata that is stripped before game creation, deterministic start-game through injected shuffle seeds, fixture bypass into immediate game setup, nil-game-safe pre-game view models, deterministic app handler initialization from an injected shuffle seed, no implicit demo pieces in the production app-db boundary, explicit demo-piece seeding with reconciled stashes, derived current player/board/pieces/card zones, overlay panel normalization/toggling, board/card/territory/move/header/help/app view-model aggregation, stored Three.js runtime status driving board renderer availability, selected territory updates, stable board-index selection/targeting across destroyed-territory gaps and newly created territory indexes, the move-selection facade, all-major power option audits, explicit unavailable transition fallback, move source availability including no-piece draw rejection, staged controls, incomplete selections, draw-card confirmation through pure game state, stale draw confirmation rejection after pieces disappear, full-hand draw discard selection, deterministic draw reshuffle confirmation from an injected shuffle seed, Fool/High Priestess/Judgement browser staging through draw-major facades, Cup territory, enemy-piece, wasteland, unbounded, and Wheel draw-pile confirmations, Rod minion movement, Rod unbounded full-destination movement, Rod piece pushing, Rod territory pushing from board-click targets, Disc hand-card piece growth, Disc territory growth from hand, Star discard-pile replacement plus minion-orientation controls, Strength two-action controls, Justice/Death/Tower/Moon Sword-major staging, Sword piece and territory attack staging with damage, replacements, Tower discard-pile replacement, survivor orientation, and rejected confirmation preservation, Sun Cup-then-Disc created-piece and created-territory browser staging, ordered composite suit-major browser staging for Empress territory sources and Lovers hand-card sources, World browser staging for copied composite, copied Death Sword-major, and Magician suit powers plus invalid copy targets, Hierophant hand-card browser staging, orient-piece confirmation, initial-small placement into wasteland targets, rejected selections without game mutation, and draw/discard derivation without top-level card-zone state [16][17][18][23][24][35][37][42][43][44][45][46][47][50][51][53]. Game-schema, game-state, and feature tests cover setup validity, deterministic construction, deck accounting, stash mirror and active-piece accounting, transition result maps, standalone draw/discard success, discard-pile reshuffle, invalid draw commands, Fool reveal plus optional implemented suit-power play, High Priestess two-pass redraws, Judgement discard-pile draws including hand-source self-draw and hand-limit rejection, Cup success paths including enemy-piece creation, variant validation, unbounded placement, Wheel draw-pile territory creation, and rejection, Rod variant normalization/rejection helpers, Disc variant/target/replacement command validation helpers, Sword source/variant/target/damage/replacement command validation helpers, Sword piece attack shrink/destroy success/rejection paths, Sword territory shrink/destroy success/rejection paths including Tower discard-pile replacements and voided-piece cleanup, Disc piece-growth and territory-growth success/rejection paths, Hierophant replacement and missing-stash rejection, Hermit piece/territory relocation, Devil retargeting after orientation, Disc Gherkin coverage for hand-source discard, Star discard-pile replacement, Strength shortcuts, and missing-resource/enemy-occupied rejections, Star orient-before-Disc targeting, Sun Cup-then-Disc created-piece sequencing, World copy enumeration/delegation/rejection paths, pure Rod minion movement, piece pushing, territory pushing, pip limits, destination rejection, unbounded full-destination success, owned/enemy orientation rules, orient and initial-placement success/rejection cases, structured rejection cases, and facade-to-focused-namespace compatibility for draw, draw-major, Cup, Rod, Disc, Sword, manipulation, World, and placement transitions [17][19][20][35][36][40][41][42][43][46][47][50][51][53].
[30] **Icon Layout and Rendering Modes** - `src/main/gnostica/icon_layout.cljc` centralizes the shared 2x special-move icon scale, DOM overlay dimensions, Three.js canvas texture dimensions, and fit checks for up to three stacked icons [21][26]. `src/main/gnostica/icon_view.cljs` owns shared inline SVG icon rows so shared card faces, the CSS fallback, Three.js popovers, and the icon guide use labels from `gnostica.icons` instead of renderer-local text [21][22][23][25][26]. `src/main/gnostica/three_card_textures.cljs` owns Three.js card image cover fitting, canvas-backed card texture creation, Canvas icon mark drawing, and smoke-visible metadata for supported icon count and layout fit [26][31][32]. `gnostica.icons` also owns ordered glossary entries with detailed descriptions for all special move icons, `gnostica.app-state` stores UI-only `:card-icon-mode` plus icon-guide visibility, `gnostica.ui.header` exposes the Icons toggle and icon guide control, `gnostica.keyboard-shortcuts` declares the `I` and `G` global shortcut commands, `gnostica.app.keyboard` dispatches those commands through the re-frame listener effect, and `gnostica.ui.help` renders the keyboard command dialog [22].
[31] **Icon Verification Paths** - Always mode renders card-face icon stacks; popup mode hides card-art overlays and reveals icon-plus-label descriptions on hover/focus for board, hand, and discard cards [23][30]. `gnostica.three-card-textures` validates and bakes icon stacks into canvas textures in always mode, while `gnostica.three-board` remounts with plain card textures in popup mode, clears stale texture-error status through queued re-frame dispatch, preserves the active OrbitControls view, and shows a DOM icon description popover for focused or hovered board cards [12][13][26][30]. Test and smoke coverage for icon behavior lives in its own node [39].
[32] **Smoke Runner Coverage** - `gnostica.smoke-runner` starts the released Ring app unless `SMOKE_URL` targets a running server, so local smoke runs require the compiled browser bundle at `src/main/resources/js/main.js` to be current, normally by running `clojure -M:release` first [15]. `gnostica.smoke.browser` owns reusable Chrome discovery/lifecycle, DevTools websocket commands, waits, diagnostics, page setup, and input dispatch, while `gnostica.smoke.page-stats` owns Gnostica JavaScript snippets, screenshot pixel checks, readiness predicates, and fixture query URLs based on `gnostica.fixtures` constants [44]. The runner orchestrates desktop/mobile Three.js checks, full-viewport canvas sizing, r128 globals, velvet table metadata, black void pixels, nonblank screenshots, antialias metadata, nine texture loads, card texture icon metadata, wasteland outlines, visible piece counts matching piece edge outlines including a smoke-only wasteland piece, card zones, reset control, keyboard camera movement, `?`/`G` dialogs, `I` view preservation, center-card selection, blocked-CDN fallback with a wasteland piece marker, and fake-revision fallback [9][11][12][13][14][15][22][25][30].

[33] **Piece Rendering Surfaces** - Three.js renders pieces as lit `THREE.ConeGeometry` pyramid meshes with `MeshLambertMaterial`, black edge outlines, pale pip markers, upright/cardinal orientation, and board-layout height/rotation helpers [12][16][18][27]. The CSS fallback renders matching colored triangular or diamond markers with clipped pip marks and lists pieces in the selected-territory panel [9][25].
[34] **Game Setup State** - `create-game` accepts explicit player specs plus injected `:deck-order` or deterministic `:shuffle-fn`, enforces two to six known player ids backed by `gnostica.pieces/players`, rejects malformed or duplicate deck cards, and treats the resulting deck order as authoritative for the whole setup [8][16][17]. It deals each player a six-card hand, calls `gnostica.board/initial-board` with `identity` on the remaining cards for the territory grid, and keeps the post-board remainder in `:draw-pile`, preserving the order hands, then board, then draw pile [8][19]. Optional `:starting-bids` and public `apply-starting-bids` accept `:rounds`/`:bid-rounds` plus `:redraws`, remove one bid card per player per round, rank majors by `icons/major-arcana-card-ids` above all minors, rank minors king through ace, require another full-table round when the top bid ties, redraw all bid cards from the revealed pool counterclockwise starting to the winner's right, record bid history/redraw metadata, rotate players/turn order so the winner starts, and preserve six-card hands plus unique deck accounting [7][21][36]. The state records phase, players, `:players-by-id`, turn order, board, pieces/stashes, discard pile, setup/bid hooks, and history [19].
[35] **Cup Move Transition** - `gnostica.game-state.cup/apply-cup-move`, re-exported as `gnostica.game-state/apply-cup-move`, returns `{:ok? true :state ... :events [...]}` or structured `{:ok? false :error ...}` maps [17]. Cup commands require `:player-id`, a territory or hand-card source plus acting piece, carried `:cup-variant`, and a target of `:territory`, `:piece`, or `:wasteland` [23][24]. Territory targets create the current player's small piece with a selected orientation; enemy piece targets create one of that enemy's small pieces on the target territory while preserving the target orientation; wasteland targets create territories from `:territory-card-source :hand` plus `:one-point-card-id`, or Wheel-only `:draw-pile-top`. Plain Cup and Magician wild-suit Cup enforce the three-piece limit, Empress-style `:cup-unbounded` ignores it, unavailable variants and non-Wheel draw-pile requests are rejected explicitly, hand-card sources move the source card to discard, and app-state confirmation resets successful selections while preserving rejected ones with error data [29][37].
[36] **Schema Invariants and Helpers** - Whole-game predicates check player counts, unique player ids, `:players-by-id` consistency, turn membership, current-player indexing, piece ownership by participating players, exactly one piece location shape, territory piece references to board cells, wasteland piece coordinates against `gnostica.board-layout/wasteland-spaces`, official setup target scores, stash ownership, player/piece stash mirror equality, active-piece plus stash totals per player/size, board coordinate/orientation consistency, and unique card ids across hands, board, draw pile, and discard pile [16][17][18][19]. Public helpers are `valid-game?`, `explain-game`, `assert-valid-game`, and `invariant-errors`; feature steps use readable explanations without coupling to Three.js or re-frame state [20][29].
[37] **Move Selection Flow** - Three.js raycast card picks and CSS fallback card buttons dispatch through the shared app-state board-selection event for inspection, then `gnostica.move-selection/select-board-for-active-move` stages existing-territory choices by stable board cell `:index` so destroyed-territory gaps and newly appended territory indexes remain addressable; Cup enemy-piece targets, Cup and initial-placement wasteland choices, Wheel territory-card source choices, Rod target-piece choices, ordered composite major Cup/Rod/orient/hand-trade steps, draw discard-card choices, Fool reveal counts, High Priestess redraw passes, Judgement discard-pile card choices, Strength Disc action counts, Star minion orientation, Disc resolver-backed piece/territory growth plus replacement-card choices, Sun Cup-then-Disc mode and replacement choices, Sword resolver-backed piece/territory attacks with damage plus replacement-card choices, World copied-major territory and copied-power choices, Hierophant/Devil target-piece choices, and Hermit target/destination choices are exposed from `gnostica.ui.move-panel` [14][18][24][25][46][47][50][51][53]. Move source availability and confirmation re-checks keep no-piece turns restricted to initial small placement, so draw/play/activate/orient cannot proceed until the current player has a board piece [7][43]. Completed draw-card confirmations call `apply-draw-move` with staged discard-card ids and draw count; completed Fool and High Priestess confirmations call their draw-major facades with staged reveal/redraw data and injected shuffle options, while Judgement confirmations call `apply-judgement-move` with staged discard-pile card ids including hand-source self-draw after source cost; completed Cup territory or hand-card confirmations carry `:cup-variant` into `apply-cup-move` for current-player piece creation, enemy-piece creation, hand one-point wasteland territory creation, or Wheel top-draw-pile territory creation; completed Rod minion movement, piece pushing, and territory pushing confirmations carry `:rod-variant` into `apply-rod-move` with staged mode, distance, target, and current-player orientation data; completed Disc piece and territory growth confirmations carry `:disc-variant` into `apply-disc-move` with replacement source/card data when needed, `:disc-actions` for staged Strength one- or two-action shortcuts, and `:minion-orientation` for staged Star orient-before-targeting; completed Sword piece and territory attack confirmations carry `:sword-variant` into `apply-sword-move` with staged target kind, damage, replacement source/card when needed, and current-player survivor orientation when applicable; completed ordered composite suit-major confirmations build `:actions` and call the Empress, Emperor, Lovers, Chariot, Hanged Man, or Temperance facade; completed World confirmations build `:copied-board-index`, copied suit `:copied-power`, or copied major action commands and call `apply-world-move`; completed Hierophant, Hermit, and Devil confirmations call their manipulation facades with staged target/destination/orientation data; completed orient and first-small-placement confirmations call `apply-orient-move` and `apply-initial-placement`; staged Sun powers build nested `:cup` plus optional `:disc` commands for Cup-only, created-piece, created-territory, existing-piece, and existing-territory Disc modes. These paths update `:game` only on success and preserve rejected selections with gameplay error data [23][29][35][41][42][43][46][47][50][51][53].
[38] **Game-State Test Details** - Game-schema, game-state, and gameplay feature tests cover generated two-through-six-player states, invalid player counts, overfull hands, duplicate cards, unknown piece owners, ambiguous or missing piece locations, invalid wasteland piece coordinates, unofficial target scores, deterministic construction, deck accounting across hands/board/draw/discard, deck injection validation, turn metadata, stash shape, official starting-bid major/minor ranking, tied-round rebids, counterclockwise bid redraws, winner-first turn rotation, explicit result maps, standalone draw/discard success and rejection cases, discard-pile reshuffle when the draw pile is exhausted, Fool reveal/play behavior, High Priestess two-pass redraws, Judgement discard-pile card selection including self-draw after hand-source cost, Cup current-player piece creation, enemy-piece creation, hand and Wheel draw-pile wasteland territory creation, Cup variant validation, unbounded placement, Rod variant validation, Disc command source/variant/target/replacement validation, Sword command source/variant/target/damage/replacement validation, Sword piece attack shrink/destroy, Justice hand-trade before Sword, Death same-target Sword shortcuts, Tower orient-before-Sword, Moon Rod-then-Sword full-territory exceptions, and Justice/Death/Moon hand-source single-action attacks, Magician wild-suit attack history, hand-source discard, missing-smaller-stash rejection, territory shrink/destroy, missing/reused/invalid territory replacement rejection, Tower discard-pile replacement plus rejected discard replacements for piece or destroyed-territory attacks, and voided-piece cleanup, Disc current-player and enemy piece growth, Disc territory growth from hand and Star discard-pile replacements, Hierophant same-size replacement and missing-stash rejection, Hermit piece relocation and territory relocation to wastelands, Devil enemy-capable retargeted orientations, Disc Gherkin scenarios for hand-source discard behavior, enemy-occupied territory rejection, missing stash/replacement resources, large-piece rejection, and Strength shortcuts, Star orient-before-Disc using the just-discarded Star as a replacement, Strength double-Disc shortcuts for piece size and territory value, Sun Cup-then-Disc created-piece sequencing, World copy enumeration/composite delegation/Magician suit delegation/rejections, major-sequence hand source cost, territory source minion scope, optional ordered powers, affected-piece minion promotion, and same-target shortcut hooks, minion movement, piece pushing, territory pushing, schema-valid transitions, pip-limited Rod distance, full/void destination rejection, unbounded full-destination success, enemy-occupied territory push rejection, own-wasteland territory landing, owned/enemy orientation rules, orient-piece updates and rejection, initial-small placement on territory and wasteland targets, and structured rejection cases [17][19][20][29][34][35][36][41][42][43][46][47][49][50][51][53].
[39] **Icon Test Details** - `gnostica.cards-test`, `gnostica.icon-layout-test`, app-state tests, and the smoke runner cover metadata completeness, ordered glossary descriptions, one-/two-/three-icon coverage, blank placeholder rejection, Cup/Rod/Disc/Sword/one-point predicates, icon fit, mode normalization/toggling, `?gnostica-smoke=major-icons`, command help, icon-guide help, view preservation, and popup descriptions across Three.js and CSS fallback paths [10][15][21][22][25][30][31][40][46][47].
[40] **Rod Command Helpers** - `gnostica.cards/rod-variants` treats Wands as `:rod`, Emperor-style `:rod-unbounded`, and Magician `:wild-suits` as distinct Rod source variants, while `rod-card?` remains the broad source predicate [21]. `gnostica.game-state.rod/resolve-rod-command`, re-exported as `gnostica.game-state/resolve-rod-command`, validates browser-free Rod commands without mutating `:game`: `:player-id`, territory or hand-card source, optional `:rod-variant`, acting minion, mode `:move-minion`, `:push-piece`, or `:push-territory`, target piece or territory coordinates, positive distance within minion pips, optional matching direction, and optional orientation only for current-player moved pieces [7][16][17]. It rejects upright minions, non-Rod sources, unavailable variants, off-axis targets, invalid directions, zero/over-pip distances, bad target shapes, and enemy-piece reorientation with structured failure maps; `apply-rod-move` consumes the normalized modes and variant, and app-state confirmation now builds those commands from staged Rod selections [29][37][41].
[41] **Rod Move Transition** - `gnostica.game-state.rod/apply-rod-move`, re-exported as `gnostica.game-state/apply-rod-move`, applies resolved Rod `:move-minion`, `:push-piece`, and `:push-territory` commands in pure game state [17][40]. It moves the acting minion or target piece in the minion's facing direction, rejects void piece destinations and full territory destinations for plain or wild-suit Rods, allows `:rod-unbounded` piece moves into territories already holding three pieces, stores territory destinations with `:space-index` and wasteland destinations with `:space {:kind :wasteland ...}`, discards hand-source Rod cards, appends `:rod/minion-moved`, `:rod/piece-pushed`, or `:rod/territory-pushed` history events with the Rod variant, permits orientation changes only for current-player moved pieces, preserves enemy orientation, rejects territory pushes involving enemy-occupied target territories or landing wastelands, leaves pieces at the source coordinate, moves landing own wasteland pieces onto the territory, returns pieces whose coordinates become void after territory relocation to their stashes, and updates pushed territory orientation from the board pattern [7][16][29][36][37].
[42] **Draw Move Transition** - `gnostica.game-state.draw/apply-draw-move`, re-exported as `gnostica.game-state/apply-draw-move`, applies the standalone draw/discard turn option in pure game state [7][17][23]. Commands require `:player-id` and `:draw-count`, may include `:discard-card-ids`, and accept optional `:shuffle-fn` for deterministic tests. The transition rejects non-current players, duplicate or missing discard card ids, negative/non-integer draw counts, and requests beyond the six-card hand limit or available draw plus discard cards; on success it discards selected hand cards, draws into the current hand, reshuffles the discard pile when the draw pile is exhausted, appends `:draw/cards-drawn`, preserves unique card accounting, and is called from app-state draw confirmations [29][37][38]. The same namespace now also owns the pure draw/discard major powers for Fool, High Priestess, and Judgement [50].
[43] **Orient and Initial Placement Transitions** - `gnostica.game-state.placement/apply-orient-move`, re-exported as `gnostica.game-state/apply-orient-move`, turns one current-player piece to a legal orientation without moving it and appends a `:piece/oriented` history event [17][24][37]. `gnostica.game-state.placement/apply-initial-placement`, re-exported as `gnostica.game-state/apply-initial-placement`, applies the special no-pieces turn rule by placing the current player's small piece from stash onto an empty territory or empty wasteland, rejecting occupied targets, void targets, existing current-player pieces, missing small stash pieces, and non-current players; app-state labels this source as Place first piece, builds commands from staged orient and first-small-placement selections, and preserves rejected confirmations [7][16][29][38].
[44] **Demo and Smoke Fixtures** - `src/main/gnostica/fixtures.cljc` is the sole production-path namespace for local fixture data: `default-browser-lobby-player-specs`, `lobby-init-options`, `shared-local-controller`, `shared-local-control-init-options`, `demo-board-pieces`, `demo-board-pieces-for`, `demo-init-options`, smoke-only `smoke-wasteland-piece`/`smoke-board-pieces`, the `gnostica-smoke=major-icons` constants, `major-icon-smoke-deck-order`, `smoke-init-options`, and ClojureScript-only `browser-init-options` [6][16][17][32]. `gnostica.app/init` opts into these browser init options: the normal local path starts in a two-player lobby, `?gnostica-dev-shared-control=1` tags the default lobby seats with one local dev controller while leaving `create-game` player specs unchanged, and smoke query options bypass the lobby with deterministic six-player setup, smoke board pieces including one wasteland piece, and deck order for renderer verification. `gnostica.app-state/initialize` remains fixture-free by default and only applies board pieces when callers pass `:demo-board-pieces`; optional lobby local-controller metadata is UI-only and stripped before pure game construction. Tests and smoke helpers require this namespace directly so demo/smoke setup does not leak into `gnostica.pieces`, pure game construction, or view rendering namespaces [10][28][29].
[45] **Event Randomness Boundary** - `src/main/gnostica/app/events.cljs` registers `:gnostica.app/initialize`, `:gnostica.app/start-lobby-game`, and `:gnostica.app/confirm-move` as `reg-event-fx` handlers with an injected `:gnostica.app/shuffle-seed` coeffect, keeping browser randomness outside `reg-event-db` paths [6][17][24]. `src/main/gnostica/deterministic_shuffle.cljc` converts that seed into a deterministic Fisher-Yates shuffle function, and `src/main/gnostica/app/handlers.cljc` applies it only when immediate initialization or lobby start lacks an explicit `:deck-order` or `:shuffle-fn`, or when draw confirmation needs a transition `:shuffle-fn` for discard-pile reshuffle [23][29][34][42]. Tests call the handler namespace directly with fixed seeds to prove the same injected inputs produce identical app-db results [29].
[46] **Disc Command and Growth Helpers** - `gnostica.cards/disc-variants` treats Coins minors as `:disc`, Magician as `:wild-suits`, Star as `:disc-from-discard`, and Strength/Sun as normal Disc sources; `card-point-value` and `card-worth-one-more?` model spot-to-royalty-to-major territory growth steps [7][21]. `gnostica.game-state.disc/resolve-disc-command`, re-exported as `gnostica.game-state/resolve-disc-command`, validates browser-free single Disc commands without mutating `:game`: current player, territory or hand-card source plus acting minion, optional `:disc-variant`, target piece or territory, optional replacement-card source/id for territory growth, and optional orientation only for current-player piece growth. Targeting follows rules text: upright minions target their current space, cardinal minions target the adjacent space, and a minion can always target itself; the resolver rejects non-Disc sources, unavailable variants, enemy piece reorientation, enemy-occupied territory targets, unsupported replacement sources, bad target shapes, and off-axis targets with structured failures. `gnostica.game-state.disc/apply-disc-move`, re-exported as `gnostica.game-state/apply-disc-move`, applies resolved piece targets by replacing small pieces with medium pieces or medium pieces with large pieces, requiring the target owner's larger stash piece, returning the old piece to that stash, preserving enemy orientation, allowing validated current-player reorientation, discarding hand-source Disc cards, appending `:disc/piece-grown`, and preserving schema/card/stash accounting. It also applies resolved territory targets by replacing the target cell's card with a selected +1 card from hand or Star's discard-pile option, discarding the old territory and hand-source Disc cards, preserving pieces and orientations on the target coordinate, rejecting missing/reused/invalid replacement cards, appending `:disc/territory-grown`, and preserving schema/card accounting. Strength commands can supply `:disc-actions` with one or two action maps; same-target shortcuts grow small pieces directly to large or spot territories directly to majors while charging the source once. Star commands can supply `:minion-orientation` so orientation happens before Disc target validation and hand-source Star can be used as the discard-pile replacement after it is discarded. Browser move selection stages Strength one- or two-action counts, filters two-action territory replacements by +2 value, stages Star minion orientation before target choices, and builds those major commands for `apply-disc-move`. `gnostica.game-state/apply-sun-move` applies Sun's `:cup` action followed by an optional `:disc` action; same-created-target shortcuts can create a medium piece without a small stash piece or create a royalty territory without spending a one-point card. Browser move selection exposes `:sun` as a staged Cup-then-optional-Disc power with Cup-only, created-piece, created-territory, existing-piece, and existing-territory Disc modes, while the card's separate Cup and Disc powers remain selectable [17][29][38].
[47] **Sword Command Helpers** - `gnostica.cards/sword-variants` treats Swords minors as `:sword`, Magician as `:wild-suits`, Tower as `:sword-from-discard`, and Justice/Death/Moon as normal Sword sources [7][21]. `gnostica.game-state.sword/resolve-sword-command`, re-exported as `gnostica.game-state/resolve-sword-command`, validates browser-free single Sword commands without mutating `:game`: current player, territory or hand-card source plus acting minion, optional `:sword-variant`, target piece or territory, positive `:damage`, optional replacement-card source/id for non-destroying territory attacks, and optional orientation only for current-player surviving piece targets. Targeting follows the shared rules text: upright minions target their current space, cardinal minions target the adjacent space, and a minion can always target itself. The resolver rejects non-Sword sources, unavailable variants, zero or over-pip damage, over-target damage, enemy piece reorientation, enemy-occupied territory targets, replacement cards for destroyed territories, unsupported replacement sources, discard-pile replacements except for Tower Sword, bad target shapes, and off-axis targets with structured failures. `gnostica.game-state.sword/apply-sword-move`, re-exported as `gnostica.game-state/apply-sword-move`, applies resolved piece attacks by shrinking survivors to the target owner's appropriate smaller stash piece, destroying zero-pip victims, returning replaced or destroyed pieces to that owner stash, preserving enemy survivor orientation, allowing resolver-validated current-player survivor orientation, discarding hand-source Sword cards once, appending `:sword/piece-shrunk` or `:sword/piece-destroyed`, and preserving schema/card/stash accounting. It also applies resolved territory attacks by replacing surviving territories with a selected lower-value card from hand or Tower's discard-pile option, rejecting missing/reused/invalid replacements, discarding the original territory, preserving pieces and orientations on surviving territory coordinates, destroying territories at exact-value damage, returning any pieces left in void to owner stashes, appending `:sword/territory-shrunk` or `:sword/territory-destroyed`, and preserving schema/card/stash accounting. Composite Sword-major commands now live in the same browser-free namespace: Justice uses `:hand-trade-target` or `:hand-trade-target-piece-id` to trade post-source-cost hands before Sword; Death uses one or two `:sword-actions` with same-target shortcuts that skip intermediate piece sizes or territory values; Tower uses `:minion-orientation` before Sword; and `gnostica.game-state/apply-moon-move` applies optional `:rod` then optional `:sword` actions, including Moon's temporary full-territory Rod entry that must leave no more than three pieces after Sword. Browser move selection exposes both the single Sword attack subset and dedicated Justice, Death, Tower, and Moon staged powers that collect completed first actions under `:major-actions`, stage the next trade/Sword/Rod/orient controls, and confirm through `apply-sword-move` [17][28][29][37][38].

[48] **Scoring and Endgame Flow** - `gnostica.game-state.core` exposes scoring and end-turn helpers through the `gnostica.game-state` facade [17]. `scores`, `player-score`, and `with-current-scores` count only territories occupied exclusively by one player's pieces, valuing spot, royalty, and major cards at 1, 2, and 3 points [7][21]. `create-game` accepts only target scores 8, 9, or 10 and stores the chosen value under `[:setup :target-score]`; the local lobby exposes that option before start [6][24]. `end-turn` validates the current active player, refreshes scores, can announce a final-turn challenge when no unresolved challenge exists, resolves a challenger's next completed turn as `:game/won` or `:challenge/failed`, discards eliminated hands, returns eliminated pieces to stashes, skips eliminated players when advancing turns, and marks the last active player as winner if a failed challenge leaves only one active player [7][17][19]. `gnostica.app-state/game-status`, `header-view`, and `src/main/gnostica/ui/header.cljs` derive score chips, target score, active challenge, winner, and end-turn/challenge button state from `:game`; move source availability rejects further moves once the game phase is `:finished` [23][37].

[49] **Major Arcana Sequencing Engine** - `src/main/gnostica/game_state/major.cljc`, re-exported through `gnostica.game-state`, provides the browser-free foundation for ordered optional multi-power major actions from the rules notes [7][17]. `resolve-major-source` validates current-player major hand-card or territory sources; hand sources make all owned active pieces minions and set up one source-card discard, while territory sources make owned pieces touching that card minions. `apply-major-sequence` accepts a command with `:player-id`, `:source`, and optional ordered `:actions`, plus a spec with `:power-order`, `:apply-action-fn`, optional source card ids, optional copied `:power-card`, optional action-minion extraction, and optional same-target shortcut hooks. It charges hand sources once before action callbacks, accepts any subsequence of the fixed power order, rejects reversed or unavailable powers, promotes owned pieces named by action results' `:affected-piece-ids` or `:affected-pieces` into later-step minions, threads copied power cards into lower suit helpers for World, and lets shortcut hooks consume adjacent same-key actions for Strength/Sun/Death/Chariot-style shortcuts [38][53]. Concrete major flows call existing Cup/Rod/Disc/Sword/manipulation/composite callbacks and use `major-action-source` plus `major-paid-source-opts` so played hand cards are not double-charged [35][40][46][47][51][52].

[50] **Draw Major Powers** - `gnostica.game-state.draw` now implements pure draw/discard major facades re-exported from `gnostica.game-state`: `apply-fool-move`, `apply-high-priestess-move`, and `apply-judgement-move` [17][42]. All three use `resolve-major-source`, charge hand sources once, preserve structured success/error maps, append history events, and keep card accounting unique across hands, board, draw pile, and discard pile [19][49]. Fool commands carry `:reveals` with up to two reveal maps, move each revealed draw-pile card into discard, optionally route an implemented suit-power play command through Cup/Rod/Disc/Sword source-opts without double-charging the revealed card, and accept deterministic `:shuffle-fn` for exhausted draw piles. High Priestess commands carry up to two `:redraws`, each with `:discard-card-ids` and `:draw-count`, applying sequential discard/redraw passes after source cost. Judgement commands carry an acting `:piece-id` plus selected discard `:card-ids`, enforce the major-source minion scope, minion pip count, and six-card hand limit, and let hand-source Judgement draw itself after its source card enters discard. Browser move selection stages Fool reveal counts as skipped reveals, High Priestess redraw pass discard/draw choices, and Judgement discard-pile card choices through the existing app event shuffle boundary; nested Fool revealed-card play remains pure-only until a staged browser subflow exists [7][24][29][37][38].

[51] **Manipulation Major Powers** - `gnostica.game-state.manipulation`, re-exported through `gnostica.game-state`, implements browser-free `apply-hierophant-move`, `apply-hermit-move`, `apply-devil-move`, and shared `target-coordinate` validation for non-suit major targeting [17][49]. Hierophant replaces a target piece with the current player's same-size stash piece and orients the replacement; Hermit moves a target piece to an empty territory/wasteland or moves a target territory to an eligible wasteland while preserving source-space pieces, returning pieces that become void after territory relocation to stash, and applying own/enemy orientation rules; Devil applies up to three piece orientations, including enemy pieces, with retargeting after each action. Browser move selection exposes these through manipulation controls, builds the matching commands, discards hand sources once through major sequencing, preserves structured rejection data, and tests cover pure transitions plus Hierophant hand-card staging [24][29][37][38].

[52] **Composite Suit Major Powers** - `src/main/gnostica/game_state/composite.cljc`, re-exported through `gnostica.game-state`, implements browser-free `apply-empress-move`, `apply-emperor-move`, `apply-lovers-move`, `apply-chariot-move`, `apply-hanged-man-move`, and `apply-temperance-move` on top of the major sequencing engine [17][49]. Commands carry `:player-id`, a major `:source`, and ordered `:actions`, with convenience fields for orient/Cup/Rod/hand-trade cases. Empress orients then applies `:cup-unbounded`; Emperor orients then applies `:rod-unbounded`; Lovers applies Rod then Cup with promoted moved pieces; Chariot applies Rod twice and has a same-current-player-piece shortcut that can pass through full territories or void but cannot end there; Hanged Man applies Rod then trades hands with the targeted piece owner; Temperance applies Cup twice. Cup and Rod source opts allow promoted major minions to keep acting after leaving a source territory. Browser move selection exposes these six card ids as explicit ordered major powers, stores completed first actions in `:major-actions`, stages the next Cup/Rod/orient/hand-trade controls, and confirms through the composite facades; unit tests cover all six pure facades plus Chariot's shortcut and representative browser staging for Empress and Lovers [24][29][35][40].

[53] **World Delegation** - `src/main/gnostica/game_state/world.cljc`, re-exported through `gnostica.game-state`, implements browser-free `world-major-territories` and `apply-world-move` for the World card [7][17]. Commands keep World as the paid `:source`, choose a non-World major territory with `:copied-board-index`, and then carry the copied card's usual fields such as `:power`, `:actions`, `:cup`, `:disc`, `:sword-actions`, targeting, damage, replacement, distance, or orientation data. The resolver rejects missing, minor, or World copy targets, uses `resolve-major-source` so hand World grants all owned pieces and territory World grants only pieces on World, passes the copied card as `:power-card` into Cup/Rod/Disc/Sword and major-sequence helpers, delegates to existing draw, suit, Sun, Sword-major, composite, and manipulation implementations, and preserves World as the source card for cost and event summaries [35][40][46][47][49][50][51][52]. Browser move selection now stages World by listing `world-major-territories`, validating copied-power choices for multi-power copied majors like Magician or Empress, reusing existing child controls, and confirming through `apply-world-move` [24][29][37].
