# Mind Map Format - Self-Documentation

> **For AI Agents:** This mind map is your primary knowledge index. Start with the Gnostica project anchors [6-10], then follow links [N] to find what you need; consult format nodes [1-5] only when you need the mind-map rules. Always reference node IDs. When you encounter bugs, document your attempts in relevant nodes. When you make changes, update outdated nodes immediately—especially project overview nodes since they're your springboard. Add new nodes only for genuinely new concepts. Keep it compact (20-50 nodes typical). The mind map wraps every task: consult it, rely on it, update it. Never change the first 5 numbered points.

> **Project Navigation Note:** In this Gnostica file, nodes [1-5] are preserved generic format references, not the project's daily navigation hubs. For project work, start at Gnostica anchors [6-10]; any generic wording inside [1-5] about overview nodes describes the mind-map format in general, not this repository's project anchors.

[1] **Mind Map Format Overview** - A graph-based documentation format stored as plain text files where each node is a single line containing an ID, title, and inline references [2]. The format leverages LLM familiarity with citation-style references from academic papers, making it natural to generate and edit [3]. It serves as a superset structure that can represent trees, lists, or any graph topology [4], scaling from small projects (<50 nodes) to complex systems (500+ nodes) [5].

[2] **Node Syntax Structure** - Each node follows the format: `[N] **Node Title** - node text with [N] references inlined` [1]. Nodes are line-oriented, allowing line-by-line loading and editing by AI models [3]. The inline reference syntax `[N]` creates bidirectional navigation between concepts, with links embedded naturally within descriptive text rather than as separate metadata [1][4]. This structure is both machine-parseable and human-readable, supporting grep-based lookups for quick node retrieval [3].

[3] **Technical Advantages** - The format enables line-by-line overwriting of nodes without complex parsing [2], making incremental updates efficient for both humans and AI agents [1]. Grep operations allow instant node lookup by ID or keyword without loading the entire file [2]. The text-based storage ensures version control compatibility, diff-friendly editing, and zero tooling dependencies [4]. LLMs generate this format naturally because citation syntax `[N]` mirrors academic paper references they've seen extensively during training [1][5].

[4] **Graph Topology Benefits** - Unlike hierarchical trees or linear lists, the graph structure allows many-to-many relationships between concepts [1]. Any node can reference any other node, creating knowledge clusters around related topics [2][3]. The format accommodates cyclic references for concepts that mutually depend on each other, captures cross-cutting concerns that span multiple subsystems, and supports progressive refinement where nodes are added to densify understanding [5]. This flexibility makes it suitable as a universal knowledge representation format [1].

[5] **Scalability and Usage Patterns** - Small projects typically need fewer than 50 nodes to capture core architecture, data flow, and key implementations [1]. Complex topics or large codebases can scale to 500+ nodes by adding specialized deep-dive nodes for algorithms, optimizations, and subsystems [4]. The methodology includes a bootstrap prompt for generating initial mind maps from existing codebases automatically [1]. Scale is managed through overview nodes [1-5] that serve as navigation hubs, with detail nodes forming clusters around major concepts [3][4]. The format remains navigable at any scale due to inline linking and grep-based search [2][3].

[6] **Gnostica App Overview** - Gnostica is a Clojure/ClojureScript implementation of the tarot-and-Icehouse territory game documented in `docs/rules.txt` [7]. Browser startup lives in `src/main/gnostica/app.cljs`; stable re-frame keyword ids live in `src/main/gnostica/app/ids.cljc`, public UI ids are re-exported from `src/main/gnostica/app/events.cljs`, and focused registration side effects live under `src/main/gnostica/app/registrations/` [9][22][23][24][42][43]. Shared `.cljc` namespaces hold board, the `gnostica.app-state` facade plus focused internals under `src/main/gnostica/app_state/`, gesture-intent, move-selection, game-state, schema, layout, legal-target presentation, icon, keyboard shortcut, piece, fixture, and major sequencing contracts [8][16][17][18][19][21][24][47]. The browser surface combines local lobby setup, full-window Three.js board with CSS fallback, card/move/territory overlays, score/end-turn/challenge controls, keyboard help, and special move icon guide [11][12][15][22][23][24][25][30][46]. Implemented gameplay includes setup/bids, draw/discard, Cup/Rod/Disc/Sword moves, major powers, World delegation, orient/initial placement, and endgame flow, with browser staging routed through gesture intents or move selection and explicit unavailable results for unwired powers [34][35][37][39][40][41][44][45][48]. Direct manipulation work should start from the checked-in interaction spec [49]. Workflow starts by consulting MIND_MAP.md project anchors [6-10], leaving preserved format nodes [1-5] unchanged, and updating stale nodes as part of code or docs changes; AGENTS.md carries that mind-map loop and clarifies that `cs` is for search/exploration while exact line-numbered context should use normal file reads. Work then uses Jujutsu plus `br`: start from `br ready`, inspect with `br show <id>`, run `clojure -M:lint` for clj-kondo/cljfmt plus the br tracker guard, and sync tracker changes with `br sync --flush-only`; `.beads/embeddeddolt` is a tracked regular-file sentinel that blocks legacy Go `bd` from treating this br-only repo as an embedded-Dolt workspace, and accidental `.beads/issues.jsonl` removal is recovered with `br sync --flush-only --force` before `br doctor` and `br sync --status`.

[7] **Rules Source** - `docs/rules.txt` is the local implementation rules reference, while the upstream Gnostica PDF at `https://www.looneylabs.com/sites/default/files/pyramid_rules/Gnostica.pdf` is retained as provenance. If those sources differ, treat `docs/rules.txt` as authoritative for current repo behavior until it is deliberately reconciled against the PDF. Implemented slices include deterministic setup, target scores and starting bids, six-card hands, nine-card territory setup, card-zone accounting, draw/discard, scoring/challenge/victory, Cup/Rod/Disc/Sword moves and variants, major powers including World delegation, orient-piece, initial placement, and browser confirmation wiring for implemented staged powers [8][9][17][23][24][34][35][37][39][40][41][44][45][46][48].

[8] **Board Model** - `src/main/gnostica/board.cljc` defines the direct initial 3x3 board helper. When called directly, `initial-board` shuffles the provided deck with its default shuffle or an injected shuffle function before taking nine table cards, records each cell's row, column, `:face :up`, and assigns checkerboard orientation with top-left portrait so every orthogonal neighbor is opposite [7][10]. The authoritative `create-game` setup path orders or shuffles the full deck once, deals player hands first, then calls `initial-board` with `identity` on the remaining board deck so board setup does not double-shuffle or consume cards before hands [17][34].

[9] **Board Rendering** - `src/main/gnostica/ui/board.cljs` renders the board area from the composed `:gnostica.app/board-view` subscription, chooses between `gnostica.three-board/scene` and the CSS fallback, passes renderer callbacks, dispatches stable selection events exposed through `src/main/gnostica/app/events.cljs` and registered by `src/main/gnostica/app/registrations/events.cljs`, and renders an empty pending board state safely before the lobby has created `:game` [11][12][13][14][17][25]. `board-view` caches board cells, pieces, pieces-by-space keyed by `gnostica.pieces/territory-space` or `wasteland-space`, wastelands, board-space bounds, selected index, move legal-target descriptors, icon mode, Three.js runtime status, renderer errors, and derived renderer availability so fallback layout, capability decisions, and target highlighting stay out of the view render body [16][18][24][25][29]. `src/main/gnostica/app.cljs` composes a local lobby screen before game start, then a full-window board shell with closable overlay card zones and side-docked move/territory panels driven by `:open-panels` [6][23][24]. The normal board surface is `gnostica.three-board/scene`, with Three.js runtime gating, camera controls, raycast selection, and browser-free layout math split across focused nodes [11][12][13][14][18][26][27]. The CSS `.board-stage` path remains the fallback board surface with a matching responsive velvet-to-black fade, board-card pieces, wasteland piece markers, selected territory state, and legal/disabled target classes aligned with the 3D path [25]. The surrounding UI lets the board keep the viewport, derives card zones from `:game`, and stages move choices outside `:game` [23][24][37].

[10] **Test Runner** - `clojure -M:lint` runs `dev/gnostica/verify.clj`, which first checks the br tracker guard for a regular-file `.beads/embeddeddolt` sentinel and present `.beads/issues.jsonl`, then invokes clj-kondo and cljfmt over `src/main`, `test`, `deps.edn`, and `shadow-cljs.edn` while excluding generated `src/main/resources/js` assets [6]. `clojure -M:test` runs `gnostica.test-runner`, which discovers every non-smoke `*_test.clj` namespace under `test/gnostica`, requires them, and then runs the discovered namespaces in deterministic order [8][16][17][18][19][20][22][28][29][42][50]. Unit coverage is grouped by pure rendering math, card metadata, fixture data, piece geometry, app-db derivations, shortcut command matching, shared spatial helper behavior, game-state transitions, command/result contracts, schema invariants, feature-runner scenarios, and runner discovery regression tests [17][20][22][28][29][38][42][50]. App-state behavioral tests are split under `test/gnostica/app_state/`, game-state move-family tests are split under `test/gnostica/game_state/`, and shared support under `test/gnostica/test_support/` provides reusable deck, board, piece, game-state, app-db, app-state, and move-family helpers [29][38]. Browser release and smoke coverage for the Three.js board, fallback paths, card zones, icon mode behavior, and registered move-panel control rendering live outside the unit runner [15][31][32].

[11] **Three.js CDN Runtime** - `src/main/resources/index.html` loads Three.js plus `examples/js/controls/OrbitControls.js` from pinned `three@0.128.0` CDN URLs before `/js/main.js` with SRI hashes and `crossorigin="anonymous"`, preserving the npm-free shadow-cljs workflow [6]. `gnostica.three-board.runtime/runtime-status` requires `THREE.REVISION` `"128"` and `THREE.OrbitControls`, and `src/main/gnostica/app/registrations/events.cljs` captures that status through a re-frame coeffect during initialization so `:gnostica.app/board-view` drives Three.js versus CSS fallback rendering from app-db data [9][25][43]. `src/main/externs/three.ext.js` declares the global and advanced-compilation APIs used by the 3D renderer [12][13][27].

[12] **Three.js Board Scene** - `gnostica.three-board/scene` is the public Reagent component that renders the 3D board from shared board cells and piece state instead of duplicating game data [8][9][16][18]. Its implementation is split under `src/main/gnostica/three_board/`: `runtime.cljs` gates the CDN globals, `resources.cljs` creates the WebGL renderer and disposes renderer/listener/material resources, `controls.cljs` owns camera metadata, OrbitControls configuration, reset, and keyboard panning, `pointer.cljs` owns raycast card selection plus object drag/drop picking for card planes, piece meshes, and wasteland target planes, `scene_graph.cljs` builds table/card/wasteland/piece meshes from stable territory and wasteland space keys, `accessibility.cljc` builds the live board aria-label from the current cells including zero/one/many territory wording and selected popup icon summaries, and `lifecycle.cljs` orchestrates mount/update/unmount around those contracts while coloring card, wasteland, and piece highlight planes from legal-target descriptor status [11][13][14][24]. Scene metadata counts only pieces whose current space is a drawable territory or wasteland, so smoke can compare visible piece count to rendered edge outlines [15][32]. Card texture composition remains in `gnostica.three-card-textures`, while renderer detail behavior is documented in focused nodes [26][27][30].

[13] **3D Camera Controls** - `gnostica.three-board.controls` configures the z-up perspective camera and `THREE.OrbitControls` used by `gnostica.three-board/scene`, keeping the default view over the 3x3 board [11][12]. Controls enable mouse, trackpad, and touch orbit/zoom, disable pointer pan, clamp distance and polar angle to allow closer card inspection without clipping through pieces, expose zoom clamp plus camera-distance/target metadata for smoke verification, support focused WASD/arrow-key bounded board-plane panning when no drag is active, preserve the active camera distance and target across icon-mode, board cell, piece, and direct-manipulation remounts, and expose a `Reset view` button that restores the saved default camera state [9][15][22].

[14] **3D Pointer Selection** - `gnostica.three-board.scene-graph` assigns each card mesh a board index and shared gesture object payloads from `gnostica.gesture-input`, while `gnostica.three-board.pointer` uses `THREE.Raycaster` on click-like pointer releases to pick card planes after camera movement and invokes callbacks supplied by `gnostica.ui.board` so territory inspection and gesture intents route through stable `:gnostica.app/...` events [8][9][12][37]. Card, piece, and wasteland target meshes carry stable territory indexes, piece ids, or wasteland coordinates; card clicks select territory cards, active wasteland target clicks stage the same wasteland target selection as CSS and Detailed entry, object drags suppress OrbitControls only for the active drag candidate, empty-table gestures keep orbit/zoom behavior, pointer drag previews expose legal/disabled target status without mutating `:game`, and drag distance filtering prevents normal orbit gestures from changing selection [13][24][49].

[15] **3D Board Verification** - The CDN-based 3D board path is verified with `clojure -M:lint` for the br tracker guard plus source lint/format drift, `clojure -M:test` for pure board/layout/piece invariants, `clojure -M:release` for advanced compilation against `src/main/externs/three.ext.js`, and `clojure -M:smoke` for automated browser coverage [10][11][16][18]. The default smoke path starts the released Ring app and therefore needs a current `src/main/resources/js/main.js` from `clojure -M:release`; `SMOKE_URL` can target an existing dev or release server instead [32]. Smoke coverage is split between reusable Chrome/CDP harness code, Gnostica page-stat/readiness checks, and high-level scenario orchestration for Three.js runtime readiness, nonblank rendering, card zones, icon toggles, card selection, registered move-panel control rendering, confirmed direct-drop and wasteland-click mutation coverage, and fallback paths [25][31][32].

[16] **Icehouse Piece Model** - `src/main/gnostica/pieces.cljc` defines six metadata-backed player colors, piece sizes/pips, legal orientations, stable `territory-space` and `wasteland-space` keys for grouping active pieces, compatibility helpers for territory-index lookups, and square-pyramid geometry helpers for cardinal pieces lying on a side face [6][10][18]. Pieces with `:space-index` group under territory keys and pieces with `:space {:kind :wasteland ...}` group under wasteland coordinate keys, letting app-state, CSS fallback, and Three.js render both legal occupancy forms [9][12][25][29]. Local visible demo pieces live in `gnostica.fixtures/demo-board-pieces`, smoke-only wasteland coverage lives in `gnostica.fixtures/smoke-board-pieces`, and `gnostica.app-state` injects them into `[:pieces :on-board]` only when explicit init options provide `:demo-board-pieces`, keeping player stashes and `[:pieces :stashes]` reconciled through `gnostica.game-state/with-board-pieces` [9][17][36][42]. `gnostica.ui.board` passes the derived board-piece collection into the renderer; Three.js and CSS rendering details live in focused nodes [27][33].

[17] **Game State Contract** - `src/main/gnostica/game_state.cljc` is the browser-free public facade for tests, Gherkin-style feature steps, and re-frame events, while `src/main/gnostica/game_state/core.cljc` owns deterministic construction, deck accounting, players, turns, pieces/stashes, draw/discard piles, target score, starting bids, scoring, challenge/end-turn resolution, the shared turn-action eligibility guard, setup hooks, and compact history [6][10][19][34][46]. `validate-board-pieces` and `with-board-pieces-result` reject impossible fixture/test seeds such as duplicate piece ids, unknown owners, invalid territory/wasteland locations, or stash-overflow counts before `with-board-pieces` replaces active pieces and rebuilds both stash mirrors [16][36][42]. Shared pure helper namespaces `gnostica.game-state.spatial` and `gnostica.game-state.card-source` centralize row/col normalization, cardinal targeting, target summaries, piece-space movement, and discard-pile card lookup so Cup/Rod/Disc/Sword/manipulation/composite rules do not drift on those basics [35][37][39][44][45][48]. Focused transition namespaces return explicit success/error maps: Cup [35], Rod [39], draw/discard [40], placement [41], base Disc plus Disc major orchestration [44], base Sword plus Sword major orchestration [45], reusable major sequencing [47], and concrete draw/manipulation/composite/World major powers [48]. `gnostica.game-state` also re-exports browser-free command/result contract helpers from `gnostica.game-state.command-contracts` so callers can check public command shapes without coupling to UI state [50]. The public facade wraps turn-action transitions so unknown, non-current, eliminated, finished, and no-piece non-placement turn attempts fail before mutation, re-exports `resolve-starting-bid-rounds` for lobby previews that share official bid ranking and tie behavior before final `apply-starting-bids`, and exposes stable APIs used by app initialization, lobby start-game/bidding, move confirmation, and end-turn/challenge transitions; `gnostica.app.handlers` injects deterministic shuffle functions for browser setup, lobby bidding setup, and draw reshuffles [9][23][24][37][42][43][46].

[18] **Board Layout Helpers** - `src/main/gnostica/board_layout.cljc` contains browser-free renderer math shared by the Three.js board scene, CSS fallback, app-state/move-selection board-index lookup, and tests [9][10][12][24]. It defines card dimensions, territory gaps, table size, table fade margins/planes, `cells-by-index`/`cell-by-index` stable board-index lookup, card centers, wasteland detection, board-space bounds, outline segments, overflow-aware visible piece slots and CSS marker positions, side-face pip marker descriptors, compass rotations, side-face roll, card surface height, and separate upright/cardinal piece contact heights so lying pieces sit on the card while upright pieces keep their visual lift [12][16][26][27]. Keep deterministic placement math here; leave DOM, WebGL, OrbitControls, and raycasting in the `gnostica.three-board.*` renderer namespaces, and card image fitting plus Canvas icon texture composition in `gnostica.three-card-textures` [11][13][14][30].
[19] **Gameplay State Schemas** - `src/main/gnostica/game_schema.cljc` adds Malli-backed schemas for pure gameplay data using `metosin/malli` from `deps.edn` [17]. It defines reusable schemas for card references, board cells including post-setup territory growth or destruction, six-card hands, card piles, player state with score/challenge/elimination flags, Icehouse pieces with exactly one territory or wasteland location, turn metadata, official target scores, setup data including expected deck ids, winner data, history events, and the whole `GameState` map [8][16][17][46]. Whole-state invariant predicates and public validation helpers are split out for scanability, including readable errors for ambiguous/missing piece locations, stale wasteland coordinates, unsupported target scores, expected deck-card accounting, and broader state-accounting failures [36]. Separate Malli command-shape contracts live in `src/main/gnostica/game_state/command_contracts.cljc`, where they stay structural and leave rule-specific validation to the transition namespaces [50].
[20] **Gameplay Feature Harness** - `features/` contains Gherkin-style gameplay rule scenarios run by `test/gnostica/gameplay_feature_test.clj` through `gnostica.feature-runner` [10][17][19]. `test/gnostica/feature_world.clj` centralizes deterministic setup, identity deck order, fixture seeding, pure action application, state lookup, deck accounting, and Malli explanations, while `test/gnostica/feature_steps.clj` maps Given/When/Then text to helpers [17][19][35][39][44][45][46][48]. Feature discovery runs every `.feature` file under `features/` through `run-feature-file`, and the runner returns structured failures for empty feature files, zero-step scenarios, missing or empty `Scenario Outline` examples, malformed headings or table rows, unresolved outline step placeholders, and steps outside Background or Scenario sections so parser regressions cannot pass vacuously. Current scenarios cover setup, Cup, Rod, Disc, Sword, draw-major, manipulation-major, and endgame examples, with detailed coverage summarized in the test nodes rather than repeated here [10][29][38].
[21] **Gnostica Icon Stacks** - `src/main/gnostica/icons.cljc` is the browser-free source for Stickas major-arcana icon stacks, icon ids, labels, and the `GnosticaStickas.pdf` reference URL [30]. `src/main/gnostica/cards.cljc` attaches each major card's ordered one-, two-, or three-icon `:gnostica-icons` vector while minors deliberately have none, and it also exposes bid rank, Cup, Rod, Disc, Sword, one-point, and territory point-value helpers used by gameplay helpers [17][28][34][39][44][45]. Layout, rendering modes, hotkey integration, and verification are documented separately so icon-stack updates do not require editing one dense node [22][30][31].
[22] **Keyboard Command Help** - `gnostica.app-state` keeps browser-free `:hotkey-help-open?` and `:icon-help-open?` flags plus open/close helpers as a public facade, with pure header/help view-model helpers implemented in `src/main/gnostica/app_state/view_models.cljc`; `src/main/gnostica/keyboard_shortcuts.cljc` defines the shared hotkey command catalog plus pure global key matching, `src/main/gnostica/ui/header.cljs` renders header controls and the mobile compact game-context chip from `:gnostica.app/header-view`, `src/main/gnostica/ui/help.cljs` renders both modal dialogs from `:gnostica.app/help-dialogs-view` and the selected command list, and `src/main/gnostica/app/keyboard.cljs` owns the global keydown listener controller registered through the `:gnostica.app/install-keyboard-shortcuts` re-frame effect path [6][9][21][30][43][46]. `src/main/gnostica/app.cljs` wraps header/game chrome in `.app-modal-scope` and marks it inert plus `aria-hidden` while help is open; `ui/help.cljs` focuses the close control, traps Tab/Shift+Tab, closes on Escape or overlay/close clicks, and restores previous focus; `app/keyboard.cljs` suppresses app-level global shortcuts while a help dialog is open except Escape close; `:gnostica.app/help-dialogs-view` includes dev-only commands only when app-db `:dev-demo-hotkeys?` is true [9][15][32][42]. Supported browser shortcuts are `?` to show keyboard commands, `G` to show detailed special move icon explanations, `I` to toggle card icon overlays without resetting the current 3D OrbitControls view, board-local WASD/arrow keys to move the focused 3D view when no orientation-capable drag or keyboard first-placement targeting is active, Enter/Space on Place first piece to start keyboard target selection on the center territory, arrow keys to move that target among legal empty territories and wastelands, Enter/Space to accept and later confirm, active-drag arrow keys to set eligible dragged-piece orientation, first-placement arrow/`O` hotkeys to update orientation after pointer drop or accepted keyboard target, `O` to cycle dragged or pending first-placement orientation, and `Esc` to close open help dialogs or cancel active keyboard placement targeting; when browser init includes `?gnostica-dev-demo-hotkeys=1`, `D` dispatches `:gnostica.app/layout-shuffled-deck-territories` to rewrite only the dev app-db view so all configured deck cards appear as territories, hand/draw/discard zones are cleared, staged move state is reset, and pure gameplay transitions stay untouched; keep the shared shortcut catalog, visible command list, keyboard listener, unit tests, modal smoke checks, README, and AGENTS notes synchronized when adding shortcuts [10][13][15][21][49].
[23] **Current-Player Card Zones** - `gnostica.app-state/card-zones` derives the current hand, draw pile, discard pile, counts, and top discard card from the authoritative `:game` value rather than duplicating zones at the app-db top level [9][17]. The lower-level `:card-zones` subscription is registered internally under `gnostica.app.registrations.subscriptions`, while `gnostica.app.events` exposes the composed `:gnostica.app/card-zones-view` boundary that combines current player, icon mode, zone data, and move legal-target descriptors for `gnostica.ui.card-zones` [6][24][29]. `gnostica.ui.card-zones` renders a closable bottom overlay with hand card images/titles, a facedown draw deck affordance with remaining count, and a discard pile empty state or top card while reusing shared card-face helpers from `gnostica.ui.card`; active move descriptors add legal/disabled styling and reason tooltips, and visibility is controlled by `:open-panels` rather than duplicating card data [6][9][30][37]. Standalone draw confirmations call `apply-draw-move`, which can discard selected hand cards, draw toward the six-card limit, and reshuffle the discard pile when the draw pile is exhausted; Cup hand-card moves discard through `apply-cup-move`, Rod hand-card minion, piece, and territory moves discard through `apply-rod-move`, and Disc hand-card growth moves discard through `apply-disc-move` via app-state confirmation [35][37][39][40][44].
[24] **Move Selection UI** - `gnostica.app-state` keeps staged selection outside `:game` under `:move-selection`, gesture metadata under `:gesture-intent`, keyboard-only first-placement target mode under `:keyboard-placement-targeting`, direct-manipulation preferences under `:direct-manipulation`, consumed-turn metadata under `:turn-action`, and overlay state under `:open-panels` [9][17][37][49]. Focused app-state internals keep ownership narrow: `app_state/lobby.cljc` is the local-lobby compatibility facade, `app_state/lobby_setup.cljc` owns lobby seat normalization/editing/validation, `app_state/lobby_bidding.cljc` owns official bid staging state, `app_state/lobby_start.cljc` owns casual and official game-start app-db transitions, `app_state/lobby_view_models.cljc` owns lobby view-model composition, `app_state/view_models.cljc` composes non-lobby board/card-zone/territory/move-panel/header/app/help data, `app_state/gestures.cljc` bridges direct gesture intents, drag orientation, keyboard first-placement target navigation, and the pending-move tray view, and `app_state/moves.cljc` re-exports stable move-selection facade functions while owning confirmation and end-turn app-db plumbing. `:gnostica.app/move-panel-view` composes current player, prompt, ready state, canonical footer action state, selected power, board cells, legal-target descriptors, direct settings, ordered `:control-groups`, and `move-action-ribbon` for the detailed move panel; `:gnostica.app/pending-move-tray-view` is a separate public composed subscription that merges `gnostica.gesture-intent/pending-move-tray-view` with `move-action-ribbon` for gesture-started pending Confirm, Cancel, Detailed entry, missing-choice, and error state; `gnostica.legal-targets` centralizes descriptor CSS classes, reason extraction, active/selected predicates, drag-preview status fields, and card/territory/wasteland/piece lookup indexes used by board, card zones, move panel, pending tray, and Three.js; `gnostica.ui.move-panel` is the public UI facade, with focused `gnostica.ui.move-panel.source`, `.choices`, `.controls`, `.ribbon`, `.pending-tray`, and `.shell` namespaces rendering source picking, reusable choices, detailed power controls, informational action ribbons, pending tray, and footer Cancel/Confirm controls from the appropriate composed subscription [29][37][48]. `gnostica.app.subscriptions/move-panel-view` and `pending-move-tray-view` delegate the browser re-frame subscriptions directly to app-state facades so subscription parity tests do not depend on long positional input wiring [29]. `gnostica.gesture-intent` stages direct object intents for hand cards, territory cards, draw/discard piles, pieces, and stash pieces onto the same `gnostica.move-selection` state machine, recording inferred source/target, missing choices, preview results, alternatives, and structured errors without mutating `:game` [37][49]. `gnostica.move-selection` remains the public facade for staged state, source/power/minion/target updates, previews, action ribbons, confirmation, cancellation, consumed-action, and error/result derivations; `gnostica.move-selection.registry` centralizes source/power labels, card-to-power mapping, control kinds, renderer control keys, command builder keys, transition facades, and previewability while focused `.staging`, `.options`, `.controls`, `.targets`, `.commands`, `.confirmation`, `.ribbon`, `.preview`, and `.contexts` namespaces own staged-param reducers and stale-param invalidation, option/prompt metadata, option/control groups, legal-target descriptors, command construction, confirmation-side transition dispatch/app-db result application, action-ribbon view models, board preview view models, and helper context construction behind that facade. `gnostica.move-selection.contexts` centralizes helper context construction while each helper namespace keeps required dependency keys and explicit ex-data failures for missing or non-callable facade dependencies. `app-state` stays the app-db facade: it resolves board choices by stable cell `:index`, passes legal targets through board/card-zone/move-panel view models, opens inspection and Detailed entry panels, clears consumed/gesture/keyboard-targeting state after successful transitions, and accepts injected draw-major or Wheel Cup shuffle options from `gnostica.app.handlers` [18][35][39][43][44][45].
[25] **CSS Board Fallback** - The `.board-stage` CSS board remains usable when required CDN globals are unavailable, the loaded Three.js revision is not r128, or WebGL renderer creation fails [9][11]. It preserves row/column card placement, the matching velvet-to-black mat fade, faint dotted wasteland outlines, visible color/orientation/pip piece markers on territories and occupied wastelands, selected-territory clicks, active wasteland target clicks, CSS piece click/drag affordances for current-player orient starts and active minion/target choices, and legal/disabled target styling from the same descriptors used by Three.js; initial-placement target descriptors remain hit-testable while `:highlight?` suppresses blanket legal-square painting, and drag-hover window listeners mount through explicit Reagent lifecycle hooks so CSS target highlights stay analyzer-friendly [14][16][24][37][49]. The smoke runner covers blocked CDN scripts, injected revision drift, and a smoke-only wasteland piece marker so fallback behavior stays aligned with the 3D path [15][32][42].
[26] **Three.js Card and Wasteland Rendering** - `gnostica.three-board/scene` renders nine face-up card planes with `/images/*.png` textures over a canvas-textured table plane that remains velvet under playable spaces and fades to black beyond wastelands, requests WebGL antialiasing, exposes actual context antialias metadata, uses white card material plus sRGB texture/output encoding from `gnostica.three-card-textures`, rotates landscape meshes 90 degrees, and spaces card centers with `gnostica.board-layout/card-position` so orthogonal cards never touch [7][12][15][18][30]. It draws faint dashed line outlines for browser-free `wasteland-spaces` below cards and pieces using r128 `BufferGeometry`, `Line`, and `LineDashedMaterial`, and adds invisible wasteland target planes plus hidden highlight planes so raycast drops and legal/disabled target feedback can use the same stable wasteland coordinates as the CSS fallback [14][18][37].
[27] **Three.js Piece Meshes** - Three.js renders Icehouse pieces as lit four-sided cone meshes over occupied cards, with size, color, up/cardinal orientation, child black `EdgesGeometry`/`LineSegments` outlines, and child circle pip markers derived from `gnostica.pieces` plus `gnostica.board-layout` side-face descriptors [12][16][18]. Lying/cardinal pyramids are rolled and tilted so the contact face sits flush above the card while preserving compass direction and card raycast selection [14][16][18].
[28] **Core Unit Test Coverage** - Card tests verify the 78-card manifest, classpath image resources, major icon metadata, Cup/Rod/Disc/Sword/one-point predicates, and territory point values [21][39][44][45]. Board tests verify nine face-up cards, injected shuffle use, and alternating orthogonal orientations [8]. Fixture tests verify demo piece metadata/capacity/filtering, overflow-space fixture coverage, browser lobby init options, dev demo hotkey fixture gating, smoke lobby bypass options including the smoke wasteland piece, and the major-icons smoke deck order, while board-layout and piece tests cover renderer coordinates, wasteland detection, board-space bounds, board-index lookup, stable territory/wasteland piece grouping, overflow-aware visible piece slots, pip marker counts/positions, compass rotations, piece height math, legal sizes/orientations, and lying square-pyramid side-face geometry [16][18][42].
[29] **Game and App-State Test Coverage** - App-state tests live in focused namespaces under `test/gnostica/app_state/`, covering initialization, dev demo deck layout gating/accounting, lobby setup and bidding, registry metadata, view models, legal targets, gesture intent, move-panel ribbons/control groups, staged-param clearing, canonical footer action readiness, draw staging, keyboard-only first-placement target navigation/cancel/confirmation, and Cup/Rod/Disc/Sword/Sun/World/major/orient/initial-placement browser confirmations [16][17][18][23][24][37][40][42][43]. `gnostica.test-support.app-db`, `.app-state`, and `.deck` hold reused app-db board/piece target lookup, player-hand mutation, shared app-state fixture pieces, helper summaries, board-card placement, and deck-order builders; move-panel renderer coverage now asserts registry/control-renderer data while the renderer implementation is split under `ui/move_panel/` rather than concentrated in one source file [10][24][37]. Direct-manipulation tests cover pointer-drag settings, Detailed-entry default behavior, gesture staging/cancel/detail continuation, command equivalence with staged controls, replacement-card drops, Disc/Sword target-kind inference, Star/Tower discard-source descriptors, and shared gesture-input payloads for CSS, card-zone, move-panel, and Three.js pointer paths [14][23][25][37]. Browser-staging tests cover representative success and rejection paths while game-schema, split game-state tests, and feature tests cover setup validity, deck/stash accounting, result maps, transition rules, major delegation, scoring/endgame flow, schema-valid transitions, and facade compatibility [17][19][20][35][36][38][39][41][44][45][46][48].
[30] **Icon Layout and Rendering Modes** - `src/main/gnostica/icon_layout.cljc` centralizes the shared 2x special-move icon scale, DOM overlay dimensions, Three.js canvas texture dimensions, and fit checks for up to three stacked icons [21][26]. `src/main/gnostica/icon_view.cljs` owns shared inline SVG icon rows so shared card faces, the CSS fallback, Three.js popovers, and the icon guide use labels from `gnostica.icons` instead of renderer-local text [21][22][23][25][26]. `src/main/gnostica/three_card_textures.cljs` owns Three.js card image cover fitting, canvas-backed card texture creation, Canvas icon mark drawing, and smoke-visible metadata for supported icon count and layout fit [26][31][32]. `gnostica.icons` also owns ordered glossary entries with detailed descriptions for all special move icons, `gnostica.app-state` stores UI-only `:card-icon-mode` plus icon-guide visibility, `gnostica.ui.header` exposes the Icons toggle and icon guide control, `gnostica.keyboard-shortcuts` declares the `I` and `G` global shortcut commands, `gnostica.app.keyboard` dispatches those commands through the re-frame listener effect, and `gnostica.ui.help` renders the keyboard command dialog [22].
[31] **Icon Verification Paths** - Always mode renders card-face icon stacks; popup mode hides card-art overlays and reveals icon-plus-label descriptions on hover/focus for board, hand, and discard cards [23][30]. `gnostica.three-card-textures` validates and bakes icon stacks into canvas textures in always mode, while `gnostica.three-board` remounts with plain card textures in popup mode, clears stale texture-error status through queued re-frame dispatch, preserves the active OrbitControls view, and shows a DOM icon description popover for focused or hovered board cards [12][13][26][30]. `gnostica.cards-test`, `gnostica.icon-layout-test`, app-state tests, and smoke cover metadata completeness, glossary descriptions, one-/two-/three-icon handling, icon fit, mode toggling, `?gnostica-smoke=major-icons`, command/icon help, view preservation, and popup descriptions across Three.js and CSS fallback paths [10][15][21][22][25][39][44][45].
[32] **Smoke Runner Coverage** - `gnostica.smoke-runner` starts the released Ring app unless `SMOKE_URL` targets a running server, so local smoke requires a current `src/main/resources/js/main.js` from `clojure -M:release` [15]. `gnostica.smoke.browser` owns Chrome/CDP lifecycle, waits, diagnostics, page setup, and input dispatch; `gnostica.smoke.page-stats` is a stable facade over focused smoke support namespaces: `.probes` for DOM/stat probe scripts, `.actions` for browser action scripts and focus helpers, `.pixels` for screenshot sampling, `.readiness` for predicates/assertions, `.urls` for fixture URL builders, and `.common-js` for shared JavaScript DOM helpers used by the probe/action snippets [42]. Coverage includes desktop/mobile Three.js readiness, viewport canvas sizing, r128 globals, table pixels, nonblank screenshots, antialiasing, texture/icon metadata, wasteland outlines, piece edge counts, card zones, desktop/mobile header context, a 320px compact turn/score smoke check, a 390x360 short-mobile gameplay reachability check for document scroll, board, card zones, and move-panel controls, direct move-entry pending trays, reset and keyboard camera controls, the `?` command dialog and icon-guide modal with focus entry, Tab/Shift+Tab containment, inert background chrome, Escape close, focus restoration, and shortcut containment while open, `I` view preservation, center-card selection, hand-card control rendering, blocked-CDN and fake-revision fallback paths, CSS fallback piece drag, keyboard-only first-placement targeting and confirmation on both Three.js and CSS boards, and no-piece first-placement drops on both renderers with Detailed entry confirmation plus pending-orientation hotkey coverage; the confirmed Three.js drop also pans/zooms first and asserts the camera survives the piece-update remount [9][11][12][13][14][15][22][24][25][30][46].

[33] **Piece Rendering Surfaces** - Three.js renders every drawable board piece, including unbounded overflow occupants, as lit `THREE.ConeGeometry` pyramid meshes with `MeshLambertMaterial`, black edge outlines, pale pip markers, upright/cardinal orientation, and board-layout slot/height/rotation helpers [12][16][18][27]. The CSS fallback renders matching colored triangular or diamond markers with clipped pip marks using the same overflow-aware slot positions, and lists pieces in the selected-territory panel [9][25].
[34] **Game Setup State** - `create-game` accepts explicit player specs plus injected `:deck-order` or deterministic `:shuffle-fn`, enforces two to six known player ids backed by `gnostica.pieces/players`, rejects malformed or duplicate deck cards, and treats the resulting deck order as authoritative for the whole setup [8][16][17]. It records `(mapv :id deck)` under `[:setup :deck-card-ids]`, deals each player a six-card hand, calls `gnostica.board/initial-board` with `identity` on the remaining cards for the territory grid, and keeps the post-board remainder in `:draw-pile`, preserving the order hands, then board, then draw pile [8][19]. Optional `:starting-bids` and public `apply-starting-bids` accept `:rounds`/`:bid-rounds` plus `:redraws`, remove one bid card per player per round, rank majors by `icons/major-arcana-card-ids` above all minors, rank minors king through ace, require another full-table round when the top bid ties, redraw all bid cards from the revealed pool counterclockwise starting to the winner's right, record bid history/redraw metadata, rotate players/turn order so the winner starts, and preserve six-card hands plus expected deck accounting [7][21][36]. Public `resolve-starting-bid-rounds` reuses that round-resolution logic for the browser lobby so it can preview revealed bids, unresolved ties, post-bid hands, winners, and redraw order before staging explicit redraw choices, which the lobby can clear and recalculate before confirmation without mutating top-level app `:game`; final lobby confirmation still calls `apply-starting-bids` with the staged rounds and staged redraws, while casual lobby start rejects active starting-bid state so stale dispatches cannot create top-level `:game` before confirmation [17][29][43]. The state records phase, players, `:players-by-id`, turn order, board, pieces/stashes, discard pile, setup/bid hooks, expected deck ids, and history [19].
[35] **Cup Move Transition** - `gnostica.game-state.cup/apply-cup-move`, re-exported as `gnostica.game-state/apply-cup-move`, returns `{:ok? true :state ... :events [...]}` or structured `{:ok? false :error ...}` maps [17]. Cup commands require `:player-id`, a territory or hand-card source plus acting piece, carried `:cup-variant`, and a target of `:territory`, `:piece`, or `:wasteland`; Wheel `:draw-pile-top` territory creation may carry deterministic `:shuffle-fn` [23][24][43]. All Cup target kinds must occupy the acting minion's target coordinate: territory targets create the current player's small piece with a selected orientation, enemy piece targets create one of that enemy's small pieces on the target territory while preserving the target orientation, and wasteland targets create territories from `:territory-card-source :hand` plus `:one-point-card-id`, or Wheel-only `:draw-pile-top`. Plain Cup and Magician wild-suit Cup enforce the three-piece limit, Empress-style `:cup-unbounded` ignores it, unavailable variants, out-of-range targets, and non-Wheel draw-pile requests are rejected explicitly; hand-card sources move the source card to discard before a needed Wheel discard reshuffle, and app-state confirmation resets successful selections while preserving rejected ones with error data [29][37].
[36] **Schema Invariants and Helpers** - Whole-game predicates check player counts, unique player ids, `:players-by-id` consistency, turn membership, current-player indexing, piece ownership by participating players, unique active piece ids, exactly one piece location shape, territory piece references to board cells, wasteland piece coordinates against `gnostica.board-layout/wasteland-spaces`, official setup target scores, stash ownership, player/piece stash mirror equality, active-piece plus stash totals per player/size, board coordinate/orientation consistency, and expected deck-card ids across hands, board, draw pile, and discard pile with readable duplicate, missing, unknown, and count-mismatch errors [16][17][18][19]. Public helpers are `valid-game?`, `explain-game`, `assert-valid-game`, and `invariant-errors`; feature steps use readable explanations without coupling to Three.js or re-frame state [20][29].
[37] **Move Selection Flow** - Board clicks from Three.js and CSS fallback first update territory inspection when a territory card is clicked, then stage active territory choices by stable board cell `:index` or active wasteland choices by coordinate so destroyed gaps, appended cells, and empty wastelands remain addressable [14][18][24][25]. Direct manipulation starts through `gnostica.gesture-intent/start-gesture-intent`, populating the same staged fields as Detailed entry and exposing pending Confirm, Cancel, Detailed entry, preview, missing-choice, and action-ribbon data without mutating `:game`; Detailed-entry default mode opens the panel automatically while preserving staged choices [24][49]. Browser gestures cover card-zone hand/draw/discard sources, board-card territory sources, Three.js card/piece/wasteland targets, CSS fallback piece/territory/wasteland targets, stash-piece first placement, replacement-card drops, active-drag and pending first-placement orientation keys, and pointer-drag disablement while click/button workflows remain available [12][14][22][23][25][29]. `gnostica.move-selection.targets/move-legal-targets` backs the facade legal/disabled descriptors for territories, wastelands, pieces, hand cards, discard cards, draw pile, and stash pieces from the same option helpers and pure resolvers used by confirmation; descriptors carry stable indexes, coordinates, selected status, error codes, and concise reasons into board, card-zone, move-panel, and Three.js view models through `gnostica.legal-targets` presentation helpers [9][12][23][24][25][29]. `gnostica.move-selection.preview` derives Rod and Hermit paths, initial-placement/orient compass state, and Disc/Sword/Sun mutation summaries for CSS and Three.js overlays without mutation, while `gnostica.move-selection.ribbon` derives ordered major, Sun, World, Fool, High Priestess, Judgement, and Devil action-ribbon steps [12][24][25][49]. Source availability and confirmation re-checks use the shared turn-action guard, no-piece placement rule, consumed-turn metadata, finished-game state, and an Activate territory check that requires an owned piece on a territory rather than only wasteland pieces [7][17][41][46]. `gnostica.move-selection.staging` owns pure source, power, target-kind, World copy, and Fool child-power param reducers so stale staged fields clear consistently before command builders run. `gnostica.move-selection.registry` supplies power metadata used by `gnostica.move-selection.controls` for option/control grouping, by `gnostica.move-selection.commands` for canonical final and resolver-probe command shapes, and by `gnostica.move-selection.confirmation` for transition facade dispatch, preview eligibility, rejected confirmation preservation, and turn-action consumption; successful results replace `:game`, while rejected results keep staged data plus structured errors [29][35][39][40][41][44][45][48].
[38] **Game-State Test Details** - Game-schema, split game-state, command-schema, and feature tests cover generated legal states plus invalid player, hand, card, piece-location, wasteland, target-score, stash, card-accounting, public command-shape, and structured result cases [19][36][50]. Focused game-state namespaces under `test/gnostica/game_state/` cover setup, endgame, facade delegation, draw, Cup, Rod, Disc, Sun, Sword, Sword-major, composite major, manipulation, placement, World, command schema, and spatial helpers [10][17][20][29]. Shared helpers in `gnostica.test-support.deck`, `.board`, `.pieces`, `.game-state`, and `.game-state-moves` own deterministic game creation, board-card deck positions, board-cell lookup, state hand mirrors, piece lookup, reusable move-family pieces, challenge helpers, and discard movement used by these tests [10][29]. Transition tests assert explicit result maps, draw/discard and draw-major behavior, Cup variants, Rod movement/pushing limits, Disc and Sword command validation plus piece/territory mutations, Sun shortcuts, Sword-related major composites, manipulation majors, World copy delegation/rejection, major-sequence source cost and minion promotion, scoring/endgame flow, orient-piece, initial-placement, facade turn-action legality rejections, schema-valid transitions, and structured rejection cases [17][20][29][35][39][40][41][44][45][46][47][48].
[39] **Rod Command and Transition** - `gnostica.cards/rod-variants` treats Wands as `:rod`, Emperor-style `:rod-unbounded`, and Magician `:wild-suits` as distinct Rod source variants, while `rod-card?` remains the broad source predicate [21]. `resolve-rod-command` validates browser-free Rod commands without mutating `:game`: current-player territory or hand-card source, acting minion, variant, mode `:move-minion`, `:push-piece`, or `:push-territory`, target coordinates, positive distance within minion pips, optional matching direction, and orientation only for current-player moved pieces [7][16][17]. `apply-rod-move` consumes the normalized command, discards hand-source Rod cards, moves minions or pushed pieces into non-void spaces while enforcing the normal three-piece limit except for `:rod-unbounded`, preserves enemy orientation, rejects enemy-occupied territory pushes, leaves pieces behind when territories move, lands moved territories under own wasteland pieces, reconciles voided pieces to stashes, updates pushed territory orientation from the board pattern, appends Rod history, and is built from staged app-state confirmations [29][36][37].
[40] **Draw Move Transition** - `gnostica.game-state.draw/apply-draw-move`, re-exported as `gnostica.game-state/apply-draw-move`, applies the standalone draw/discard turn option in pure game state [7][17][23]. Commands require `:player-id` and `:draw-count`, may include `:discard-card-ids`, and accept optional `:shuffle-fn` for deterministic tests. The public facade rejects finished, eliminated, non-current, and no-piece draw attempts through the shared turn-action guard; the transition also rejects duplicate or missing discard card ids, negative/non-integer draw counts, and requests beyond the six-card hand limit or available draw plus discard cards. On success it discards selected hand cards, draws into the current hand, reshuffles the discard pile through the shared `core/refresh-draw-pile` helper when the draw pile is exhausted, appends `:draw/cards-drawn`, preserves unique card accounting, and is called from app-state draw confirmations [29][37][38]. `core/refresh-draw-pile` rejects non-sequential or non-permutation shuffle results before mutating state, including duplicate, missing, invalid, unknown, or changed discard cards; this protects draw, Fool reveals, High Priestess redraws, and Wheel draw-pile territory creation from corrupting deck accounting [35][48]. The same namespace now also owns the pure draw/discard major powers for Fool, High Priestess, and Judgement [48].
[41] **Orient and Initial Placement Transitions** - `gnostica.game-state.placement/apply-orient-move`, re-exported as `gnostica.game-state/apply-orient-move`, turns one current-player piece to a legal orientation without moving it and appends a `:piece/oriented` history event; the public facade shares the turn-action guard for finished, eliminated, and non-current players [17][24][37]. `gnostica.game-state.placement/apply-initial-placement`, re-exported as `gnostica.game-state/apply-initial-placement`, applies the special no-pieces turn rule by placing the current player's small piece from stash onto an empty territory or empty wasteland, rejecting occupied targets, void targets, existing current-player pieces, missing small stash pieces, and non-current players; app-state labels this source as Place first piece, builds commands from staged orient and first-small-placement selections, records consumed-turn state after success, and preserves rejected confirmations [7][16][29][38].
[42] **Demo and Smoke Fixtures** - `src/main/gnostica/fixtures.cljc` is the sole production-path namespace for local fixture data: `default-browser-lobby-player-specs`, `lobby-init-options`, `shared-local-controller`, `shared-local-control-init-options`, `dev-demo-hotkeys-init-options`, `demo-board-pieces`, `demo-board-pieces-for`, `demo-init-options`, smoke-only `smoke-wasteland-piece`/`smoke-board-pieces`, the `gnostica-smoke=major-icons` and `gnostica-smoke=direct-drop` constants, `major-icon-smoke-deck-order`, `smoke-init-options`, and ClojureScript-only `browser-init-options` [6][16][17][32]. `gnostica.app/init` opts into these browser init options: the normal local path starts in a two-player lobby, `?gnostica-dev-shared-control=1` tags the default lobby seats with one local dev controller while leaving `create-game` player specs unchanged, `?gnostica-dev-demo-hotkeys=1` sets app-db `:dev-demo-hotkeys?` so the browser-only `D` shortcut can lay out every configured deck card as a territory for inspection, and `?gnostica-smoke=...` modes bypass the lobby for smoke fixtures. `?gnostica-smoke=major-icons` uses all six players, smoke board pieces including one wasteland piece, and the major-icon deck order for renderer/icon verification; `?gnostica-smoke=direct-drop` keeps the default two players, opens the cards and move panels, seeds no demo pieces, and uses identity shuffle for first-placement/direct-drop confirmation coverage [22]. `gnostica.app-state/initialize` remains fixture-free by default and only applies board pieces when callers pass `:demo-board-pieces`; optional lobby local-controller metadata is UI-only and stripped before pure game construction; dev demo hotkeys are app-db fixture behavior and must stay out of pure game-state transition namespaces. Tests and smoke helpers require this namespace directly so demo/smoke setup does not leak into `gnostica.pieces`, pure game construction, or view rendering namespaces [10][28][29].
[43] **Event Randomness Boundary** - `src/main/gnostica/app/ids.cljc` owns the `:gnostica.app/...` keyword ids, `src/main/gnostica/app/events.cljs` re-exports public UI ids, and `src/main/gnostica/app/registrations/events.cljs` remains the aggregate load point for domain-specific event registration namespaces under `src/main/gnostica/app/registrations/events/`; the lifecycle, lobby, move, and Three.js domains register `:gnostica.app/initialize`, `:gnostica.app/start-lobby-game`, `:gnostica.app/start-lobby-bidding`, `:gnostica.app/reveal-move-fool-card`, `:gnostica.app/confirm-move`, and `:gnostica.app/refresh-three-runtime-status` as `reg-event-fx` handlers with injected coeffects where needed, keeping browser randomness outside `reg-event-db` paths [6][17][24]. `src/main/gnostica/app/registrations/subscriptions.cljs` remains the aggregate load point for base, chrome, move-option, and composed-view subscription registrations under `src/main/gnostica/app/registrations/subscriptions/`; granular ids stay internal while UI namespaces subscribe through composed view boundaries, and `test/gnostica/app_events_test.clj` statically checks public exports plus event/subscription id coverage across the load-tree files. `src/main/gnostica/deterministic_shuffle.cljc` converts the seed into a deterministic Fisher-Yates shuffle function, and `src/main/gnostica/app/handlers.cljc` applies it only when immediate initialization, direct lobby start, or official lobby bidding setup lacks an explicit `:deck-order` or `:shuffle-fn`, or when draw, draw-major, or Wheel Cup draw-pile territory confirmation needs a transition `:shuffle-fn` for discard-pile reshuffle [23][29][34][35][40]. Tests call the handler namespace directly with fixed seeds to prove the same injected inputs produce identical app-db results [29].
[44] **Disc Command and Growth Helpers** - `gnostica.cards/disc-variants` identifies Coins, Magician wild suits, Star discard-pile Disc, and Strength/Sun Disc sources; `card-point-value` and `card-worth-one-more?` model territory growth [7][21]. `gnostica.game-state.disc` owns `resolve-disc-command`, base source opts, and single-action piece/territory growth helpers: it validates current-player source, acting minion, variant, piece/territory target, replacement source/card, and current-player-only orientation without mutating state. Targeting follows upright current-space, cardinal adjacent-space, and minion self-targeting rules, rejecting non-Disc sources, unavailable variants, enemy reorientation, enemy-occupied territory targets, invalid replacements, bad target shapes, and off-axis targets with structured errors [17]. `gnostica.game-state.disc-major` owns the public high-level `apply-disc-move` dispatch for Strength/Star plus plain Disc fallback, and `apply-sun-move` for Sun's Cup-then-Disc order. Base Disc growth swaps pieces or replaces territories while preserving schema/card/stash accounting; Strength uses one or two `:disc-actions` plus same-target shortcuts and promotes generated owned pieces into later-action minions; Star can orient before validation and use just-discarded Star as a replacement; Sun applies Cup then optional Disc with created-piece or created-territory shortcuts [29][35][38][48].
[45] **Sword Command Helpers** - `gnostica.cards/sword-variants` identifies Swords, Magician wild suits, Tower discard-pile Sword, and Justice/Death/Moon Sword sources [7][21]. `gnostica.game-state.sword` owns `resolve-sword-command`, base source opts, and single-action piece/territory attack helpers: it validates current-player source, acting minion, variant, piece/territory target, positive damage, optional replacement card for surviving territory attacks, and current-player-only survivor orientation without mutating state. Targeting mirrors Disc rules, while rejection covers non-Sword sources, unavailable variants, zero/over-pip or over-target damage, enemy reorientation, enemy-occupied territories, replacements for destroyed territories, unsupported replacement sources, non-Tower discard replacements, bad target shapes, and off-axis targets [17][44]. Base Sword attacks shrink or destroy pieces, replace or destroy territories, return voided pieces to stashes, and preserve schema/card/stash accounting. `gnostica.game-state.sword-major` owns the public high-level `apply-sword-move` dispatch for Justice/Death/Tower/Moon plus plain Sword fallback: Justice can trade only or trade before Sword, Death supports same-target shortcuts, Tower orients before Sword, and Moon can apply optional Rod then optional Sword with temporary full-territory entry validation [29][37][38][48].

[46] **Scoring and Endgame Flow** - `gnostica.game-state.core` exposes scoring and end-turn helpers through the `gnostica.game-state` facade [17]. `scores`, `player-score`, and `with-current-scores` count only territories occupied exclusively by one player's pieces, valuing spot, royalty, and major cards at 1, 2, and 3 points [7][21]. `create-game` accepts only target scores 8, 9, or 10 and stores the chosen value under `[:setup :target-score]`; the local lobby exposes that option before start [6][24]. `end-turn` validates the current active player, requires no-piece players to place their initial small piece before ending or announcing a challenge, refreshes scores, can announce a final-turn challenge when no unresolved challenge exists, resolves a challenger's next completed turn as `:game/won` or `:challenge/failed`, discards eliminated hands, returns eliminated pieces to stashes, skips eliminated players when advancing turns, and marks the last active player as winner if a failed challenge leaves only one active player [7][17][19][41]. `announce-challenge` delegates to the same end-turn claim path so direct callers cannot leave a same-turn unresolved challenge. `gnostica.app-state/game-status`, `header-view`, and `src/main/gnostica/ui/header.cljs` derive score chips, target score, the mobile `.mobile-game-context` current-score/target chip, active challenge, winner, turn-action visibility, and disabled end-turn/challenge button state from `:game`; the `:gnostica.app/header-view` subscription delegates to `app-state/header-view` so active games keep those controls visible while availability stays aligned with `game-state/can-end-turn?` and `game-state/can-announce-challenge?`; move source availability rejects further moves once the game phase is `:finished` [22][23][37].

[47] **Major Arcana Sequencing Engine** - `src/main/gnostica/game_state/major.cljc`, re-exported through `gnostica.game-state`, provides the browser-free foundation for ordered optional multi-power major actions [7][17]. `resolve-major-source` validates current-player major hand-card or territory sources; hand sources make all owned active pieces minions and set up one source-card discard, while territory sources make owned pieces touching that card minions. `apply-major-sequence` accepts `:player-id`, `:source`, and ordered `:actions` plus a spec containing fixed power order, action callback, optional copied power card, action-minion extraction, and same-target shortcut hooks. It charges hand sources once, rejects reversed or unavailable powers, promotes affected owned pieces into later-step minions, threads copied cards for World, and supports Strength/Sun/Death/Chariot shortcuts [38][48]. Concrete major flows call existing Cup/Rod/Disc/Sword/manipulation/composite callbacks through source opts so hand cards are not double-charged [35][39][44][45][48].

[48] **Concrete Major Powers** - Draw majors in `gnostica.game-state.draw` implement Fool reveals, High Priestess redraw passes, and Judgement discard-pile draws, all charging hand sources once and preserving card accounting; Fool reveal play routes already-discarded revealed cards through implemented suit powers and full-card major powers via `gnostica.game-state.major-power` [17][40][47]. `gnostica.game-state.disc-major` implements Strength, Star, and Sun orchestration on top of base Disc/Cup helpers [44]; `gnostica.game-state.sword-major` implements Justice, Death, Tower, and Moon orchestration on top of base Sword/Rod helpers [45]. Browser Fool staging mirrors revealed-card play by showing each committed reveal, offering Skip/Play, reusing child controls, and keeping World-copy-of-Fool on the same staged path [24][29]. Manipulation majors in `gnostica.game-state.manipulation` implement Hierophant same-size replacement, Hermit piece/territory relocation, and Devil retargeted orientations through shared major-source targeting [17][47]. Composite suit majors in `gnostica.game-state.composite` implement Empress, Emperor, Lovers, Chariot, Hanged Man, and Temperance as ordered Cup/Rod/orient/hand-trade sequences with major-sequence minion promotion and source opts; Hanged Man supports trade-only or Rod-then-trade browser and pure paths [35][39][47]. `gnostica.game-state.world` exposes copyable non-World major territories, accepts explicit suit or implemented full-major copied-power selectors, rejects selectors the copied card does not provide, and delegates copied powers to existing draw, suit, Disc-major, Sword-major, composite, and manipulation implementations while preserving World as the paid source [44][45][47]. Browser move selection stages these concrete major families explicitly, including Justice/Hanged Man major action-count controls, Devil action-count staging, ordered `:actions` confirmations for multiple orientations, and an action ribbon that represents completed, active, skipped, pending, and ready sub-actions for ordered major, World, Sun, Devil, Fool, High Priestess, and Judgement flows while routing confirmations through the matching facades [24][29][37][38].

[49] **Direct Move-Entry Spec** - `docs/direct_move_entry_spec.md` is the checked-in interaction contract for direct manipulation work [6][24][37]. Gestures remain a staging layer over `gnostica.move-selection` and `gnostica.gesture-intent`, legal/disabled feedback comes from `move-legal-targets`, `:game` is preserved until confirmation, and Detailed entry is the universal fallback for disabled, ambiguous, or keyboard-focused interaction [24][37]. Initial-placement stash-piece gestures avoid inheriting a default selected board target so every empty territory and wasteland remains hit-testable, while CSS and Three.js keep all legal drop targets active but highlight only the focused keyboard target or active hovered drop target through descriptor `:highlight?`; the detailed first-piece panel intentionally omits the territory/wasteland location list while retaining board/wasteland target selection, orientation controls, and pending confirmation; move-panel first-piece pointer drags also render a cursor-following DOM pyramid until drop so the source remains visible before the Three.js board ghost appears; keyboard activation of Place first piece starts a no-mutation target mode on the central territory where arrows move among legal empty territories/wastelands, Enter/Space accepts and confirms, and Esc cancels; `gnostica.app.keyboard` applies keyboard target movement, target acceptance, and orientation staging synchronously so immediate smoke key sequences such as Enter then ArrowRight cannot race re-frame's async dispatch queue; eligible active piece drags and pending first-placement moves carry arrow-key or `O` orientation choices through preview, pending tray, Detailed entry, and confirmation [22][37]. `gnostica.move-selection/move-preview` supplies browser-free Rod/Hermit paths, orient/placement compass data, and Disc/Sword/Sun mutation summaries to CSS and Three.js overlays [12][25][37]. Verification runs `clojure -M:test` for browser-free parity/no-mutation checks and `clojure -M:release` plus `clojure -M:smoke` for Three.js, CSS fallback, pending tray, Detailed entry, keyboard-only first-placement, wasteland click-target placement, and confirmed browser drop coverage [15][29][32]. The spec also records object grammar, visual states, ambiguity rules, touch/keyboard/reduced-motion behavior, command examples, and rollout phases.
[50] **Command Contract Schemas** - `src/main/gnostica/game_state/command_contracts.cljc` defines Malli command-shape schemas for public gameplay facade commands plus the structured result/error map shape [17][19]. It covers shared sources and targets, draw/orient/initial placement, Cup/Rod/Disc/Sword, Sun, Fool, High Priestess, Judgement, World, manipulation majors, composite majors, Moon, Death/Justice/Tower style Sword commands, and discard-pile replacement command fields; direct suit-style contracts use acting-source schemas that require source `:piece-id`, while major-source contracts can still omit it where minion scope is derived separately [35][39][40][41][44][45][48]. Command maps, nested sources/targets/actions, World delegated payload envelopes, and Fool revealed play payloads are intentionally closed over unknown keys, so typoed top-level fields and nested delegated/action fields return structured `explain-command` errors instead of being silently accepted. World `:copied-power`/`:power` selectors accept suit and implemented full-major keys; selected World payloads and Fool revealed play commands are checked against the appropriate child command shapes, while card-specific availability remains semantic resolver work. Variants remain mostly structural so semantic resolver errors still come from the transition namespaces, except that `:minion-orientation` is split out as the Star Disc/Tower Sword structural discriminator: plain Disc/Sword commands reject it unless the command is Star/Tower-shaped by hand-card source or discard-pile variant. `valid-result?` now distinguishes command-validation successes from state-changing successes, requiring public state successes to carry a map `:state` and vector `:events` rather than accepting bare `{:ok? true}` maps. `gnostica.game-state` re-exports `command-schema`, `known-command-kinds`, `valid-command?`, `explain-command`, `validate-command`, `valid-result?`, and `explain-result`; tests in `gnostica.game-state.command-schema-test` assert representative high-variance commands, delegated World/Fool malformed-shape and unknown-field failures, missing acting-minion rejections for suit commands, Star/Tower-only minion orientation, structured validation failures, structural-versus-semantic separation, and public result shape coverage [10][38].
